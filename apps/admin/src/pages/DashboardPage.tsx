export function DashboardPage() {
  return (
    <section className="grid gap-3">
      <h1 className="text-2xl font-semibold">Bem-vindo ao SystemBook</h1>
      <p className="text-muted-foreground">
        Use a árvore ao lado para montar a estrutura da documentação (seções → páginas → tabs) e
        clique numa página para editar seu conteúdo.
      </p>
    </section>
  );
}
