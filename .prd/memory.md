# Memória do projeto — SystemBook

> **TASK-95 concluída (2026-07-22, branch `fase-10-tokens-visuais`) — chrome secundário achatado (fim do "gray box"), fecha a Fase 10.** Segunda metade da limpeza `referencia-3.png` × `referencia.png`. **Fix raiz (uma linha, resolve o gap sistêmico da TASK-94):** adicionei `background-color: transparent` ao reset `.sb-admin button` no `@layer base` do `index.css`. Como a Fase 9 (TASK-75) **não importa o preflight** do Tailwind, TODO `<button>` cru do chrome herdava o fundo nativo `ButtonFace` (`rgb(239,239,239)`) — ⋮ das linhas (`RowActionsMenu`), handles de bloco (`BlockHandles` +/⋮), botão do breadcrumb, toggles de seção, botões ghost da toolbar, pills de menu não-ativas, toggle da sidebar, chip do usuário. Zerar no `base` os deixa planos em repouso; as utilities de fundo (`@layer utilities`, acima do base) continuam vencendo — então `bg-primary` (Publicar), `bg-accent` (item ativo/hover), `bg-muted` (kbd) ficam **intactos**. Escopado a `.sb-admin` → doc pública nunca tocada. **Única mudança de autor concreta (o offender que o task apontava):** o gatilho de busca (`AdminSearch.tsx`) tinha `border` (chip com caixa) → removido, virou ícone + "Buscar" borderless com hover-only (estilo Zeroheight), preservando alvo ≥44px mobile, `kbd ⌘K` e `aria-label`. **Nota:** o `bg-transparent` explícito que pus no `createLinkClass` (TASK-94) ficou redundante com o reset, mas **mantido** — é defensivo/auto-documentado, sem custo. **Verificação (Playwright contra o CSS buildado real, `index-BsCTYgER.css`):** 6 controles achatados medem `rgba(0,0,0,0)` em repouso (eram `rgb(239,239,239)`); search sem borda (`0px`); **fills preservados** — pill ativa `oklch(0.985)` (bg-accent), Publicar `oklch(0.546 262)` (bg-primary); hover reintroduz `bg-accent` após a transição (feedback + a11y mantidos). Screenshot `task95-shot.png` confirma o register plano. Admin typecheck/lint/build limpos. `passes:true`. **Fase 10 (incl. polimento TASK-94/95) COMPLETA — falta só abrir o PR e mergear na `main`** (branch `fase-10-tokens-visuais`).

> **TASK-94 concluída (2026-07-22, branch `fase-10-tokens-visuais`) — create-actions viram text-links de acento (estilo Zeroheight "Add page").** As quatro affordances que fazem a estrutura crescer — `InlineCreate`/'Nova seção' e `CreatePageForm`/'Adicionar página' (`SidebarTree.tsx`), `AddTab`/'+ Aba' no editor (`PageContentPage.tsx`) e `AddMenu`/'+ Adicionar menu' no header (`AppHeader.tsx`) — trocaram o resting `text-muted-foreground` cinza por um **text-link azul de acento** (`text-primary`, o único acento de ação do `plano-de-interface.md`). Estilo compartilhado num **helper único novo** `apps/admin/src/lib/styles.ts` → `createLinkClass` (reusado nos 4 pontos; padding fica a cargo de cada chamada via `cn()`): `inline-flex items-center gap-1.5 rounded-editorial-sm bg-transparent text-sm text-primary hover:underline` + focus-ring visível. Só o **gatilho colapsado** mudou; os forms expandidos (input + ✓/✕) ficaram intactos. O header ficou **icon-only** azul (`+`), consistente na linguagem de cor sem custar largura ao lado das pills. **Achado importante (corrige a especulação do technicalNotes da task):** as "caixas cinza" do `referencia-3.png` **NÃO eram build estático desatualizado** — como a Fase 9 (TASK-75) deliberadamente **não importa o preflight do Tailwind**, um `<button>` cru herda o fundo nativo `ButtonFace` (`rgb(239,239,239)`). Confirmado empiricamente via Playwright (classe original media `bg=rgb(239,239,239)`; a nova, `rgba(0,0,0,0)`). Por isso `bg-transparent` é **obrigatório** no `createLinkClass`, não redundante — sem ele o acento azul sentaria sobre um box cinza. **Implicação p/ a TASK-95:** o mesmo native-fill afeta os outros `<button>` crus do chrome (ex.: `iconBtnClass` da sidebar, triggers do `RowActionsMenu`) — "flatten the remaining chrome fills" provavelmente = adicionar `bg-transparent` a esses resting states (ou finalmente escopar um reset de `button{background:transparent}` em `.sb-admin`). Sem tocar: tint do item ativo (`bg-accent`), pill de status, botão 'Publicar'. Admin typecheck/lint/build limpos. Verificação visual: harness Playwright renderizando os 4 botões contra o **CSS buildado real** (`task94-shot.png` no scratchpad) — todos azuis, `+` na frente, zero fill. `passes:true`.

> **Backlog atualizado (2026-07-22, branch `fase-10-tokens-visuais`): 2 tasks novas de polimento na Fase 10.** O usuário comparou o CMS rodando (`referencia-3.png`) com o Zeroheight (`referencia.png`) e apontou que elementos clicáveis secundários ainda parecem "caixas cinza". Avaliação: no working tree o chrome já é majoritariamente ghost (`hover:bg-accent`, transparente em repouso) — a aparência de caixa cinza na screenshot é em boa parte **build estático desatualizado** (`apps/admin/dist`). Dois gaps *reais* vs Zeroheight sobraram: (1) as create-actions ('Nova seção'/'Adicionar página'/'+ Aba'/'+ Adicionar menu') são texto **cinza** (`text-muted-foreground`), não text-links **azuis de acento** como "Add page"/"Add tab" do Zeroheight; (2) o **gatilho de busca** ainda é um chip com `border`. → **TASK-94** (create-actions viram text-links de acento, `text-primary`) e **TASK-95** (achatar chips/bordas residuais — search sem borda + auditar/rebuild o chrome, preservando o tint sutil do item ativo, pills de status, `kbd` e o botão 'Publicar'). Ambas ui-ux, `low`, visual/CSS-only. **TASK-94 (connector no npm) foi renumerada p/ TASK-96** (`git mv`), permanecendo a **última** do backlog — a pedido do usuário. `tasks.json` + tabela de fases atualizados. Nada implementado ainda (backlog puro).

> **TASK-93 concluída (2026-07-22, branch `fase-10-tokens-visuais`) — GATE DE QA QUE FECHA A FASE 10.** Sem UI nova (mesmo papel da TASK-81 p/ Fase 9 / TASK-74 p/ Fase 8): revisão visual+funcional ponta-a-ponta do painel redesenhado. **Fan-out completo verde:** `pnpm lint` ✅, `pnpm typecheck` ✅, `pnpm -r --workspace-concurrency=1 run test` ✅ = **180 testes** (server 165, preview-kit 7, connector 8; o `✗ Build failed in 6ms` no connector é o teste negativo intencional). **Verificação no browser (Playwright/Chromium): 43 checks verdes** cobrindo os 8 steps: **(1) Não fazer** — checks programáticos: botão de ação primária (Publicar) NÃO é cinza (usa a cor primária), sidebar sem linhas de árvore (sem `border-left` em `ul`), zero bordas >2px no chrome; + screenshots (`task93-editor/home/users/public/mobile.png`) confirmam o register das referências (fundos brancos, sem caixas/cards pesados, eyebrow maiúsculo, título grande, seleção por fundo sutil, muito respiro). **(2) Menu entity e2e** — menu default "Documentação" mantém a "Get started" migrada (TASK-83); criar 2º menu + seção + página; **ativar menu reescopa a sidebar** e voltar reescopa de novo (TASK-86); navegar a página por URL resolve o menu ativo dono (`aria-current` no header); **excluir menu faz cascade** (seções/páginas somem, `pages.header` 404) atrás do `window.confirm`. **(3) Tab-relocation** — sidebar **sem** nós de aba (`a[href*="tabs/"]` ausente), "+ Aba" existe **no editor**, criar/renomear/excluir aba funcionam (nenhuma capacidade perdida na mudança da árvore→editor); **role gating** — admin vê Usuários+Tokens no user-menu, **editor logado num 2º contexto NÃO vê** Usuários/Tokens (vê Histórico) e mantém acesso à estrutura. **(4) Fluxos adjacentes** — autosave (texto entra + `data-save-status="saved"`), publish (status→"Publicado"), histórico da página lista revisão, token revelado uma vez. **(5) A11y** — nenhum botão icon-only sem `aria-label`/`title` (a toolbar do editor usa `aria-label={title}`), `⋮` focável e dropdown **operável por teclado** (Enter abre, setas navegam `menuitem`), search trigger focável. **(6) Responsivo** — mobile 390: drawer fechado por padrão, toggle abre + backdrop, zero scroll horizontal (reconfirma a TASK-92). **(7) Doc pública intocada** — `/docs` usa `.sb-public` (sem `#admin-sidebar`/chrome de admin), lista páginas publicadas, prosa Tiptap original — Fase 10 é admin-only, garantia igual à TASK-81/75. **Bug de teste (não do app) achado:** meu check lia `data-save-status` (valor cru `saved` em inglês) contra `/salvo/i` — corrigido p/ checar `saved` + texto digitado presente. **DB deixado limpo:** removidos 1 seção órfã "QA Tabs Seção" (de um run que crashou antes do teardown) e 2 tokens "QA Token"; baseline restaurado (menu Documentação + Get started + landing, 0 editores QA, só o token "test" revogado pré-existente). Scripts/screenshots no scratchpad. **Fase 10 (TASK-82..93) COMPLETA e verificada — falta só abrir o PR e mergear na `main`** (branch `fase-10-tokens-visuais` ainda não mergeada; próximo passo natural, análogo aos PRs #4–#10 das fases anteriores).

> **TASK-92 concluída (2026-07-22, branch `fase-10-tokens-visuais`):** shell do admin responsivo (plano `# Responsividade`), portando o mecanismo da doc pública (TASK-57: drawer off-canvas + backdrop + `data-open`) mas em **tokens Tailwind/shadcn** do admin (não reusei as classes `.sb-public`). **Três tratamentos genuinamente distintos** (não só um trigger mais cedo do mesmo drawer): **desktop (≥1024/lg)** = coluna fixa sticky (inalterado, toggle escondido via `lg:hidden`); **tablet (768–1023/md)** = coluna **recolhível em fluxo** (o toggle a tira do fluxo com `md:hidden`/`lg:block` e o `<main>` realarga — sem overlay/backdrop); **mobile (<768)** = **drawer** `fixed` que desliza (`-translate-x-full`↔`translate-x-0`) com backdrop `md:hidden` que fecha ao tocar. **Estado único `sidebarOpen`** no `AdminLayout` (default = `innerWidth >= 768` → tablet nasce expandido, mobile nasce fechado), lido pela mesma `aside` cujas classes trocam por breakpoint. `closeDrawerOnNavigate` só fecha abaixo de 768 (no tablet a coluna recolhível fica como o usuário deixou). **Header (`AppHeader`):** novo botão `PanelLeft` à esquerda (`lg:hidden`, 44px, `aria-controls="admin-sidebar"`/`aria-expanded`); a `MenuNav` central vira `hidden md:flex` (some no mobile p/ não dar overflow); wordmark "SystemBook" `hidden sm:inline` (o "S" basta); no `UserMenu` a engrenagem redundante some no mobile (`hidden sm:inline-flex`) e o texto do role/chevron idem. **Nav de menus no mobile** migra para um **`MobileMenuSwitcher`** (chip-row `md:hidden` no topo do drawer, só troca de menu ativo — CRUD de menu segue no header do tablet p/ cima, gap aceito). **Sem prop-drilling:** `onNavigate` chega ao `PageRow` via um `OnNavigateContext` local no `SidebarTree` (só o link que navega o consome). **Alvos de toque ≥44px no mobile** (`min-h-11 md:min-h-0`) nos links de página, no toggle de seção, no chip de menu, no gatilho de busca (`AdminSearch` trigger vira `size-11` no mobile, `sm:h-8 sm:w-auto`) e no chip do usuário. **Sem scroll horizontal:** o `AdminSearch`/shell root ganhou `overflow-x-hidden`; no editor, `.sb-editor .ProseMirror .tableWrapper { overflow-x:auto }` (o Tiptap embrulha cada tabela num `.tableWrapper` — mesma ideia do `.tableWrapper` público da TASK-57; o `pre` de código já continha overflow) + canaleta do handle de bloco reduzida p/ `2.5rem` no mobile. Admin typecheck/lint/build limpos; nenhum teste server tocado. **Verificado no browser (Playwright/Chromium, 3 viewports contra o server dev :3000):** **24 checks** — **desktop 1440** (sidebar visível, toggle escondido, MenuNav visível, zero hscroll com tabela de 9 colunas + code block de 200 chars); **tablet 820** (toggle visível, sidebar expandida por padrão + MenuNav visível, toggle **recolhe** a coluna → `<main>` alarga >100px e **sem** backdrop, reexpande, zero hscroll); **mobile 390** (MenuNav escondida, drawer fechado por padrão fora da tela + sem backdrop, toggle abre → x=0 + backdrop, selecionar página fecha o drawer, backdrop fecha o drawer via clique em coordenada exposta — gotcha da TASK-57, e ≥44px no search/toggle/link, zero hscroll com o conteúdo largo contido). Screenshots `task92-desktop/tablet/mobile-drawer.png` no scratchpad. DB deixado limpo (menu de QA removido por cascade no teardown).

> **TASK-91 concluída (2026-07-22, branch `fase-10-tokens-visuais`):** busca global do admin (plano `# Busca`), substituindo o stub desabilitado do header (TASK-85). **Backend:** novo `search.structure` (**protectedProcedure**, ao contrário do `search.query` público) em `apps/server/src/db/search.ts` → `searchStructure(db, q)`: casa **títulos** por LIKE substring (sem FTS5 — a árvore tem poucas linhas) em **menus/seções/páginas/tabs**, incluindo estrutura **não publicada** (rascunhos). Cada resultado carrega `type` + `menuId` dono (para ativar o menu na seleção) + `pageId`/`tabId` (alvo do editor) + `context` (breadcrumb curto). **Exclusões:** a tab primária/"corpo" (`isPrimary=false` no filtro — "Conteúdo" nunca vaza como aba de usuário) e a estrutura reservada da landing (`LANDING_SECTION_ID`/`LANDING_PAGE_ID`). LIKE case-insensitive só p/ ASCII (acentos não dobrados — limitação aceita, a busca de conteúdo cobre o texto real); curingas `%`/`_` do usuário são escapados (`ESCAPE '\'`). **Frontend:** novo `apps/admin/src/features/navigation/AdminSearch.tsx` — **paleta de comandos modal** (não reusei o `.sb-public` do `SearchBox` público; reconstruí com tokens do admin/shadcn), aberta pelo gatilho no header (com hint **⌘K**) ou atalho global **⌘K/Ctrl+K**. Reusa o **modelo de interação** do `SearchBox` (debounce 300ms, ↑/↓ cruzando **os dois grupos** como uma lista achatada única, Enter abre, Escape fecha) + o helper de `highlight` STX/ETX (mesma convenção, `<mark>` sem `dangerouslySetInnerHTML`). Dois grupos com eyebrow maiúsculo (**ESTRUTURA** / **CONTEÚDO PUBLICADO**, linguagem das TASK-86/87), badge de tipo por linha (MENU/SEÇÃO/PÁGINA/ABA), ícone lucide por tipo, e rodapé de dicas de teclado. **Navegação na seleção:** estrutura → `onSelectMenu(menuId)` + rota (página→`/pages/:id`, aba→`/pages/:id/tabs/:tabId`, menu/seção→`/` home, pois seções não têm rota própria — TASK-86); conteúdo → `/pages/:pageId` (**editor**, não a URL pública `/docs/…`), com o `AdminLayout` sincronizando o menu ativo dono via `pages.menuOf` já existente. Empty query = hint quieto (sem flash); sem-resultado = **`EmptyState`** `size='sm'` da TASK-90. Header (`AppHeader.tsx`) trocou o botão-stub `aria-disabled` pelo `<AdminSearch onSelectMenu={onSelectMenu}>`. **Escopo deliberadamente reduzido** (technicalNotes): componentes (vivem fora do DB), tokens (sem entidade indexável) e usuários ficam **de fora** — follow-up, não expandido. Gotchas: `rounded-editorial` não é token (só `-sm`/`-md`); reusei o índice FTS5 da TASK-53 sem duplicar. +5 testes server (`search.test.ts`: acha página não publicada por título mas não via `search.query`; casa menu/seção/aba com menu dono; não vaza primária/landing; case-insensitive + escape de `%`; UNAUTHORIZED anônimo) = **165 testes server verdes**; admin typecheck/lint/build limpos. **Verificado no browser (Playwright/Chromium 1440×900 contra o server dev :3000):** 17 checks — gatilho abre, hint sem flash em query vazia, acha página **não publicada** (estrutura), badges Seção/Aba, primária "Conteúdo" não vira aba, grupo Conteúdo publicado com snippet `<mark>` (termo só do corpo publicado), EmptyState sem-resultado, ↑/↓+Enter abre o editor, Escape/Ctrl+K, e resultado de conteúdo indo a `/pages` (não `/docs`). Screenshots `task91-palette.png`/`task91-editor.png` no scratchpad. DB deixado limpo (menu de QA removido por cascade no teardown).

> **TASK-90 concluída (2026-07-22, branch `fase-10-tokens-visuais`):** empty states para menu/seção/página/editor (plano `# Componentes > Empty State`: ilustração simples + título + descrição + botão). Novo **`apps/admin/src/components/EmptyState.tsx`** reutilizável: "ilustração" = ícone lucide num círculo suave (`bg-accent`, sem pipeline de SVG), título+descrição pelos **tokens da TASK-82** (`adminTypography`), e um slot `action` (o chamador conecta sua mutation). Prop `size='lg'|'sm'` — `sm` (compacto, tokens menores) para superfícies dentro do editor/grupo. **Três casos ligados** (busca sem-resultado fica p/ a TASK-91, não landou): **(1) Menu sem seções** e **(2) Seção sem páginas** vivem no **`DashboardPage.tsx`**, que virou a **home do menu ativo** (antes era um "bem-vindo" estático que ignorava o menu). Menu sem seções → EmptyState grande "Criar primeira seção"; com seções → lista cada seção (rótulo maiúsculo) e suas páginas como links, e uma seção **sem páginas** mostra o EmptyState compacto "Criar primeira página". **Decisão de coordenação com a TASK-86:** como seções **não são navegáveis** (são só agrupadores na sidebar, sem rota própria), o zero-state da seção não tem uma "rota de seção" — ele aparece na área principal (home). Helper local `CreateFirstButton` = botão que expande num input de título inline (só título; slug de página é derivado pelo server via TASK-70, seções não têm slug); reusa `sections.create`/`pages.create`. **(3) Página/tab sem blocos** → novo **`features/editor/EditorEmptyState.tsx`** dentro do canvas do `ContentEditor` (absolute, centrado), ação "Adicionar conteúdo" abre o **mesmo** picker de blocos da TASK-88 (`BLOCK_GROUPS` + `insertComponentEmbed`/`ComponentEmbedPicker`, sem 2º caminho de inserção); insere o 1º bloco em `insertAt=0`. **Bug real achado e corrigido na verificação:** `editor.isEmpty` do Tiptap é **baseado em texto** — inserir um heading/tabela/callout vazio (sem texto) deixava `isEmpty` true e o convite não sumia; troquei por checagem **estrutural** via `useEditorState` (vazio = exatamente um parágrafo vazio: `doc.childCount===1 && firstChild=='paragraph' && content.size===0`), reativa, some assim que o 1º bloco entra. `.sb-editor-canvas` ganhou `relative` para ancorar o overlay. Admin typecheck/lint/build limpos. **Verificado no browser (Playwright/Chromium 1440×900):** 15 checks funcionais — os 3 empty states aparecem, cada ação cria o 1º item, e **cada empty state some imediatamente após a criação** (menu→seção, seção→página, página→bloco, via a mesma invalidação de query já usada), o picker do editor traz os itens da TASK-88 (Título 1), e a home com conteúdo não mostra zero-state (sem regressão). Screenshots `task90-editor.png`/`task90-home.png` no scratchpad. DB deixado limpo (só `Documentação`; menu de QA removido no teardown).

> **TASK-89 concluída (2026-07-22, branch `fase-10-tokens-visuais`):** menu de ações contextuais nas linhas da sidebar (plano `# Ações`: "evitar botões espalhados, priorizar ações contextuais", ex. `Página ⋮ Renomear / …`). A antiga **fileira de 4 ícones sempre-visíveis** (Renomear/Mover cima/Mover baixo/Excluir, da TASK-78) em `SidebarTree.tsx` virou **um único gatilho de overflow** revelado no hover, abrindo um shadcn `DropdownMenu`. **Extraí o componente compartilhado `apps/admin/src/components/RowActionsMenu.tsx`** (como a TASK-89 pedia nas technicalNotes): consolida os **três** dropdowns quase-idênticos que existiam — a nav de menus do header (TASK-85 `AppHeader.MenuNavItem`), o tab bar do editor (TASK-86 `PageContentPage`) e agora a sidebar. Props: `triggerLabel` (aria completo), `onRename`, `onMovePrev`/`onMoveNext` (omitir esconde o item — espelha o antigo `invisible` na 1ª/última posição), `movePrevLabel`/`moveNextLabel` (default "Mover para cima"/"baixo"; o tab bar passa "esquerda"/"direita"), `onDelete` (item `variant="destructive"`), `align`, `triggerClassName` (opacity-0 group-hover… por contexto). Ícone **`MoreHorizontal` (⋯)** para bater com os menus irmãos já existentes (header/tabs) — consistência visual vale mais que o "⋮" literal do plano. **Renomear reusa a edição inline** existente (input troca a linha via `editing` state) — sem modal. Delete mantém o `window.confirm` de cascade inalterado. **`Duplicar` e "Mover entre seções" ficam de fora de propósito** — não há mutation de backend (só reorder dentro do mesmo pai); gap já flagado nas technicalNotes, não expus UI morta. Admin typecheck/lint/build limpos. **Verificado no browser (Playwright/Chromium 1440×900):** 17 checks — gatilho único por seção/página (fileira de 4 ícones sumiu), menu com Renomear/Excluir e **sem** Duplicar, "Mover para baixo" escondido na última posição, Renomear abre input inline (não modal), abrir o menu por teclado (Enter), `window.confirm` de exclusão com a cópia de cascade, e o header ainda expõe o ⋯ compartilhado. DB deixado limpo (seção QA criada e removida no teardown).

> **TASK-88 concluída (2026-07-22, branch `fase-10-tokens-visuais`):** toolbar contextual por bloco no editor (plano `# Componentes`/`# Inline Toolbar` + `referencia-2.png`). Novo **controle inline por bloco** (`BlockHandles.tsx`): ao passar o mouse sobre um bloco, revela na canaleta esquerda um **"+"** (Adicionar bloco → picker estilo lista, agrupado TEXTO/TÍTULOS/LISTAS/BLOCOS, ícone+label, igual à referência) e um **"⋮"** (Mais ações → **Duplicar / Mover para cima / Mover para baixo / Excluir**). Rastreia o bloco **top-level** sob o cursor via `posAtCoords`→`$pos.before(1)` e posiciona o handle pelo `getBoundingClientRect` do bloco relativo à `.sb-editor-canvas` (nova wrapper `position:relative`; `.ProseMirror` ganhou `padding-left:3.5rem` de canaleta). Passar sobre um parágrafo dentro de callout/tabela alinha ao bloco **inteiro** (o "+" insere depois dele, "Excluir" remove-o todo) — **sem colisão** com controles internos do NodeView (switcher do callout/dos-donts intactos). **Inserção unificada** em `blockInsert.ts` (fonte única, registry `BLOCK_GROUPS` + `insertComponentEmbed`): reusa a lógica de posicionamento de caret da TASK-32 (via `insertContentAt(atPos,…)` + rAF para os wrappers callout/dos-donts). **Ações de bloco 100% via comandos Tiptap** (sem backend novo): duplicar = `insertContentAt(node.toJSON())`; excluir = `deleteRange`; mover = transação `delete`+`insert` do próprio node PM (o Tiptap não tem "mover bloco" nativo). **Reconciliação da top toolbar (`EditorToolbar.tsx`):** o "+" inline é agora o **caminho único de inserção** — removidos os botões de inserir Tabela/Callout/Do-Don't/Embed (e o `insertBlockWithCaret`/`ComponentEmbedPicker` de lá); a toolbar guarda só formatação da linha atual (marcas negrito/itálico, toggles H1-3/listas/código) e os controles contextuais de tabela (linhas/colunas). O picker de embed abre o `ComponentEmbedPicker` existente e insere na posição guardada. Só o `ContentEditor` monta `BlockHandles`/toolbar; o render read-only público (`PageRenderer`) é intocado. Admin typecheck/lint/build limpos. **Verificado no browser (Playwright/Chromium):** 17 checks — hover revela o "+", picker com os 4 grupos, inserir Título 2/lista/callout após o bloco, **switcher interno do callout ainda funciona (sem regressão)**, ⋮ Duplicar/Excluir/Mover para baixo, e toolbar sem os botões de insert mas com H1. Screenshots `task88-picker-open.png`/`task88-hover.png` no scratchpad batem com a `referencia-2.png`. DB deixado limpo (só `Documentação`).

> **TASK-87 concluída (2026-07-22, branch `fase-10-tokens-visuais`):** área principal do editor redesenhada para ler como **documento**, não formulário (plano `# Área principal`/`# Hierarquia tipográfica`/`# Navegação` + `referencia.png`). Novos componentes em `apps/admin/src/features/editor/`: **`SectionHeader.tsx`** (eyebrow = título da seção em MAIÚSCULAS → título grande 40px `text-admin-title` + pílula de status → linha de metadados com ícone de relógio; slot `actions` recebe Publicar/Histórico, contextuais à página, **nunca** no header do app) e **`Breadcrumbs.tsx`** (Menu › Seção › Página › Aba: o Menu seleciona-se como ativo via `setActiveMenuId` e vai à raiz; a Seção é texto quieto — não tem rota própria; a Página vira link quando há aba aberta, senão é o item atual; a Aba só aparece fora do corpo). `PageContentPage.tsx` trocou o `<h1>`+ações inline pelos dois. **Metadados só do que existe no modelo:** `pages`/`sections`/`tabs` **não têm** timestamps → data/autor vêm da última revisão (`revisions.listByPage`, já retornava `criadoEm`/`autorEmail`); **status derivado** = `Publicado` se ≥1 revisão, senão `Rascunho`; sem "criado em" (não há coluna — não inventei) e **sem descrição** (não há campo de descrição por página — `SectionHeader.description` fica opcional/omitido, gap flagado). O H1 passou a mostrar o **título da página** (corrige a esquisitice pré-TASK-87 de exibir o título da primária `'Conteúdo'`). **Backend:** novo `pages.header({pageId})` (join page→section→menu num round-trip) alimenta breadcrumb+eyebrow+título; +1 teste no `structure.test.ts` (**160 testes server verdes**). **Layout:** `AdminLayout` alargou o shell p/ `max-w-[1440px]`, sidebar 280px **sticky** (`top-24`, rola sozinha), `<main>` centraliza a coluna em **`max-w-[1200px]`** com bastante respiro (`pt-24`); `AppHeader` acompanhou p/ `max-w-[1440px]`/`px-6` para alinhar com o corpo. `AdminLayout` passou a expor `setActiveMenuId` no Outlet context (novo tipo exportado `AdminOutletContext`). **Dois bugs reais achados e corrigidos:** (1) `cn()`/tailwind-merge classificava `text-admin-title` como **cor** e o descartava ao combinar com `text-foreground` → o título caía p/ o default `2em` do UA (32px); corrigido de forma durável registrando os `text-admin-*` no grupo `font-size` via `extendTailwindMerge` em `lib/utils.ts` (protege todo uso futuro de `adminTypography.*` com `cn()`); (2) publicar não atualizava o status do Section Header — o `publish.onSuccess` agora invalida `revisions.listByPage`. Novo util `lib/dates.ts` (`formatRelative` pt-BR "há 2 dias" + `formatAbsolute` no `title=`). **Verificado no browser (Playwright/Chromium 1440×900):** 19 checks (eyebrow uppercase, H1=40px=título da página, status Rascunho→Publicado após publicar, metadados "Atualizado agora mesmo por …", breadcrumbs, coluna 1200px, todos os blocos — heading/lista/code/callout/tabela — renderizando no layout largo) + 4 checks focados do breadcrumb de aba (append da aba, Página vira link com aba aberta / é o atual no corpo). Screenshots `task87-draft.png`/`task87-published.png` no scratchpad batem com a `referencia.png`. DB deixado limpo (só `Documentação`).

> **TASK-86 concluída (2026-07-21, branch `fase-10-tokens-visuais`):** sidebar redesenhada como navegação de **dois níveis** (Seção → Página) escopada ao **menu ativo**. As Tabs saíram da árvore (`TabsList` e o chevron de expansão de tabs removidos de `SidebarTree.tsx`) — agora a gestão de tabs vive **só** no tab bar do editor (`PageContentPage.tsx`): antes ele só navegava; ganhou criar (`+ Aba`, inline, aparece mesmo com 0 tabs para criar a primeira), renomear/mover-esquerda/direita/excluir por aba via `DropdownMenu` (`⋮`, hover-revealed) — nenhuma capacidade de CRUD de tab foi perdida, só relocada. `SidebarTree` agora recebe `activeMenuId` por prop (de `AdminLayout`/`useActiveMenu` da TASK-85) e consome `sections.listByMenu` (não mais `sections.list`/`LEGACY_DEFAULT_MENU_ID`); `Nova seção` cria com o `menuId` ativo. **Backend (gap descoberto):** `sections.reorder` validava contra TODAS as seções globalmente — com a sidebar escopada isso quebraria (a árvore só conhece os ids do menu). Passou a receber `menuId` e escopar `assertCompleteReorder`+updates a esse menu. Novo `pages.menuOf({pageId})` (join page→section→menu) resolve o menu dono de uma página. **Edge case "página de outro menu"** (URL/busca/breadcrumb): `AdminLayout` sincroniza o menu ativo para o dono da página aberta via `useMatch('/pages/:pageId/*')` + `pages.menuOf`, com **ref-guard por pageId** — só sincroniza na *troca de página*, então trocar de menu no header enquanto se está na mesma página **não** é sobrescrito pela sincronização. Restyle: rótulos de seção em MAIÚSCULAS/tracking (categoria, colapsável, aberto por padrão), páginas como links de nav (slug removido da árvore — lê como navegação, não file-tree), página selecionada = `bg-accent`+`font-medium` (sem borda pesada). **Add subgroup omitido** (sem modelo de dados de subgrupo — flagado no spec; não inventei estrutura de backend). Testes: +2 no `structure.test.ts` (reorder menu-scoped, `menuOf`), calls de `sections.reorder` em structure/landing/permissions test atualizados p/ `menuId` — **159 testes server verdes**; admin/server typecheck, lint e admin build limpos. **Verificado no browser (Playwright/Chromium, contra o server dev na :3000 servindo o build novo):** 13 checks do fluxo principal (menu vazio→empty-state na sidebar, criar seção/página no menu ativo, rótulo uppercase, zero chevrons de tab na árvore, página selecionada com aria-current+bg-accent, criar/renomear/excluir aba pelo editor) + 6 checks do cross-menu (nav direta a página de outro menu troca o menu ativo e a torna visível; troca no header não é sobrescrita pela sync). DB deixado limpo (só o menu default `Documentação`).

> **TASK-85 concluída (2026-07-21, branch `fase-10-tokens-visuais`):** header do admin redesenhado. Novo `apps/admin/src/features/navigation/AppHeader.tsx`: header fixo (`h-16`, `inset-x-0`), logo/nome à esquerda (link p/ `/`), nav central por `menus.list` (item ativo com fundo sutil, `+` inline p/ `menus.create`, `⋮` por item — hover-revealed `DropdownMenu` com Renomear/Mover cima/baixo/Excluir usando `menus.rename/reorder/delete` — construído aqui pois a TASK-89 ainda não existe, flagged para extração futura), e à direita busca-stub (disabled, TASK-91), engrenagem + chip de usuário abrindo o **mesmo** `DropdownMenu` controlado com Histórico global/Página inicial/Usuários(admin)/Tokens(admin)/Sair — paridade exata com a nav plana antiga. Decisão de "menu ativo" (step 1): **estado React elevado** (`useActiveMenu` hook, default = primeiro menu), não URL — documentado inline; `AdminLayout.tsx` passa `activeMenuId` via `Outlet context` para a TASK-86 consumir. `SidebarTree.tsx` foi deliberadamente **não tocado** (continua com `sections.list`/`LEGACY_DEFAULT_MENU_ID` — migração para `sections.listByMenu` é escopo da TASK-86). Componentes shadcn `dropdown-menu` e `avatar` instalados pela primeira vez no projeto (`npx shadcn add`). Sem acesso ao Chrome integrado nesta sessão para click-through visual — verificado via `typecheck`/`lint`/`build` limpos e um probe end-to-end com o `@trpc/client` real do app (login via cookie de sessão + `menus.list/create/rename/reorder/delete`) contra o server de dev, todos bem-sucedidos.

> **TASK-84 concluída (2026-07-21, branch `fase-10-tokens-visuais`):** novo router `menus` com `list/create/rename/reorder/delete` em `protectedProcedure` (admin e editor), slug derivado com desambiguação `-2/-3`, ordem e hard-delete cascade. `sections.create` agora exige/valida `menuId`, calcula ordem dentro do menu e persiste o vínculo; `sections.listByMenu` é aditivo e filtra landing, enquanto `sections.list` permanece para a sidebar legada até TASK-86. `backfillMenuSlugs` agora roda no boot. A sidebar atual cria provisoriamente no menu padrão para manter compatibilidade de tipos até ser redesenhada. Novo `menus.test.ts` cobre CRUD, reorder completo, escopo/404, cascade e autenticação. Suíte server: **137 testes verdes**; server/admin typecheck, server build e `db:generate` sem deriva também verdes.

> **TASK-83 concluída (2026-07-21, branch `fase-10-tokens-visuais`):** hierarquia persistida como `Menu → Section → Page → Tab`. Novo `menus` (UUID/título/slug nullable/ordem, índice único de slug) e `sections.menuId` obrigatório com FK cascade. As migrations **0011–0013 foram todas geradas por `db:generate`, sem edição manual**: a primeira introduz menu/coluna com default sentinela, e as duas reconstruções subsequentes que o Drizzle gerou são necessárias para promover a FK SQLite a `ON DELETE CASCADE`. `ensureDefaultMenu` cria idempotentemente `Documentação` (`__sb_default_menu__`) depois das migrations e no boot; seções legadas e a landing reservada recebem esse pai. Novo teste cobre idempotência/default e cascade. A suíte completa do server, `typecheck` e `build` passaram; `db:generate` final não reportou deriva.

> **TASK-82 em validação (2026-07-21, branch `fase-10-tokens-visuais`):** fundação visual da Fase 10 implementada no admin. `apps/admin/src/index.css` agora expõe os degraus editoriais de espaçamento (8/16/24/32/48/64), raio (8/12), sombras mínimas e a paleta neutral alinhada ao plano (#FFF/#FAFAFA/#EAEAEA/#111/#666), mantendo azul como único acento de ação. Os cinco papéis tipográficos reutilizáveis estão centralizados em `apps/admin/src/lib/typography.ts` (`adminTypography.category/title/description/metadata/body`), respaldados pelos tokens `text-admin-*`. Nenhuma estrutura de tela foi modificada; `.sb-public` e `editor.css` seguem sem alterações. `pnpm --filter @systembook/admin typecheck` e `build` passaram (apenas o warning pré-existente de chunk >500 kB). O passo 4 permanece pendente: o navegador integrado não alcançou o loopback local nesta sessão para o spot-check de todas as rotas.

> Log de desenvolvimento mantido pelo agente. Última atualização: **2026-07-21**. **Nova fase criada (2026-07-21, backlog puro): Fase 10 — redesign de interface inspirado no Zeroheight** (TASK-82..93, ver `plano-de-interface.md` e as referências visuais na raiz do repo). **TASK-82 (publicar connector no npm) foi renumerada de novo para TASK-94** (última, era TASK-71→82→92→94), a pedido do usuário, para abrir espaço 82–93 à Fase 10 — `git mv`/remap preservou o histórico. Decisões confirmadas com o usuário para a Fase 10: (1) tabs saem da árvore da sidebar (só existem dentro do editor da página); (2) links admin-only (Usuários/Tokens/Histórico global/Página inicial) migram do nav central do header para um menu de configurações/usuário; (3) **nova entidade `Menu`** foi introduzida **acima** de `sections` (hierarquia agora `Menu → Seção → Página → Tab`) — o nav central do header passa a listar menus (não seções), e a sidebar filtra pelas seções do menu ativo, trocando de conteúdo ao trocar de menu; seções pré-existentes migram para 1 menu padrão criado por backfill idempotente no boot (TASK-83); a doc pública (`/docs/...`) **não** ganha um nível de navegação por menu nesta fase (menu é admin-only, como toda a Fase 9/10). **Estado: Fases 0–7 (TASK-1..64) + conteúdo na página (TASK-65..68, PR #6) + histórico geral (TASK-69, PR #7) + auto-slug (TASK-70, PR #8) todos MERGEADOS na `main`.** **Fase 8 (TASK-71..74, bloco Dos and Don'ts) COMPLETA e MERGEADA na `main`** (PR #9, merge commit `6b61734`, branch deletada; CI 2 jobs `build` verdes, 165 testes locais verdes). **Duas novas fases criadas (2026-07-21, backlog puro, nenhum step marcado)**: **Fase 8 — bloco Dos and Don'ts** (TASK-71..74: schema/serialize → NodeView do editor → cover opcional imagem/component-embed → render público+verificação) e **Fase 9 — modernização visual do painel com Shadcn** (TASK-75..81: setup Tailwind+shadcn → lucide-react+sonner → migração por área: auth/shell, sidebar, editor, telas de gestão → QA final), ambas detalhadas no `.prd/PRD.md` §1/§4/§12. **TASK-71 (publicar connector no npm) foi renumerada para TASK-82** (última), a pedido do usuário, para abrir espaço 71–81 para as duas fases novas — `git mv` preservou o histórico do arquivo. **Reorganização (2026-07-21):** PRDs, tasks e este `memory.md` migraram de `.agent/` para **`.prd/`** (achatado: `.prd/PRD.md`, `.prd/SUMMARY.md`, `.prd/tasks.json`, `.prd/tasks/TASK-*.json`, `.prd/memory.md`); a skill `prd-creator` (movida pelo usuário p/ `.agents/skills/prd-creator/`) e o `CLAUDE.md` foram atualizados para apontar p/ `.prd`. Histórico detalhado abaixo. (Fase 6 mergeada via PR #4. Fase 7: TASK-58 ✅ (GHCR + `v0.1.0`, multi-arch p/ próximo tag), TASK-59 ✅ (compose de produção), **TASK-63 ✅** (LICENSE MIT + CONTRIBUTING.md + campo `license` nos 6 package.json) e **TASK-62 ✅** (`README.md`: positioning + "o que não é" + tabela comparativa vs Storybook/Zeroheight/Decap + quick-start self-hosting via GHCR/compose de produção + cross-link LICENSE/CONTRIBUTING — fecha o step 5/cross-link da TASK-63). **TASK-61 ✅** (`docs/preview-tsx-schema.md`: referência de `PreviewConfig`/`PreviewVariant`/`PreviewControl` a partir do tipo canônico, exemplo `button.preview.tsx` completo com 2 variantes + 1 controle de cada kind, convenção de descoberta `*.preview.tsx` e contrato postMessage `systembook:update-props`; exemplo **verificado buildando pela pipeline real** do connector; cross-link do `docs/ci-example.md` e do README). **TASK-60 ✅** (`docs/setup.md`: onboarding do zero — subir a instância via compose de produção + 4 env vars obrigatórias, primeiro login + rotação da credencial de bootstrap, instalar o conector no repo do design system + primeiro `*.preview.tsx`, gerar token de upload no painel e configurar CI; callout de single-instance/não-multi-tenant; nota honesta de pré-lançamento do connector — `private:true`, sem `bin`, ainda não no npm; labels de UI conferidos contra o código real; cross-links agora resolvidos entre setup/ci-example/preview-tsx-schema/README). Feito fora de ordem a pedido do usuário. **TASK-64 ✅** (`docs/backup.md`: backup como responsabilidade do operador, sidecar Litestream no compose de produção compartilhando o volume `systembook-data`, `litestream.yml` p/ S3-compatível + alternativa `file` local, procedimento de restore num volume vazio antes de subir o app, nota de que os previews em `/app/data/previews` não são replicados mas são reproduzíveis via CI; WAL confirmado em `client.ts`; cross-link do setup e README). **Fase 7 (TASK-58–64) COMPLETA e MERGEADA na `main`** (PR #5, merge commit `c614faa`, branch deletada). **Backlog pós-Fase 7** (features novas pedidas pelo usuário, fora das fases do PRD; numeração 65–71 após reordenação a pedido do usuário — as tasks 1–64 mergeadas ficam intactas). A feature grande (conteúdo direto na página, tabs viram opcionais) foi **quebrada em 4** em ordem de build:
- **TASK-65 ✅** (data-model, branch `feature/conteudo-na-pagina` p/ as tasks 65–68): **usuário escolheu "tab primária implícita"**. Coluna `tabs.is_primary` (boolean, default false; migration `0010_sweet_tinkerer`). Backfill na própria 0010 (raw SQL após o ALTER, precedente FTS5): promove a 1ª tab de cada página (menor `ordem`, desempate por id via `ROW_NUMBER() OVER`) a primária, e cria uma primária `'Conteúdo'` (id UUID v4 gerado em SQL) p/ páginas sem tab. Verificado com script ad-hoc (dados pré-0010: página c/ 2 tabs, página sem tab, empate de ordem) — invariante "1 primária/página" + zero blocks perdidos. `blocks.tab_id` continua NOT NULL; máquina revisions/snapshot/FTS intacta. **Gap p/ TASK-66**: o backfill cobre só páginas existentes — `pages.create` precisa criar a primária p/ páginas novas (virou step 2 da TASK-66).
- **TASK-66 ✅** (api-endpoint): pipeline do corpo da página. `pages.create` agora cria a tab primária atomicamente (transação) — toda página nova nasce com o corpo. `PageSnapshot` ganhou `isPrimary` por tab (`packages/schema/src/block.ts`); `buildPageSnapshot` ordena **primária primeiro** (`desc(isPrimary), asc(ordem), asc(id)`) porque primária e 1ª tab de usuário compartilham `ordem 0` (determinismo). **Router `tabs` passou a operar só sobre tabs de usuário** (`is_primary=false` nos where de list/create/reorder; rename/delete protegem a primária → `tabNotFound`), então user tabs seguem 0-based e a primária é intocável. Snapshot/restore/FTS já iteram todas as tabs → corpo entra automático (teste de busca do corpo). Consertei os testes que assumiam `tabs[0]`=conteúdo de usuário (agora `tabs[0]`=primária): passam a buscar por `tabId`. +3 testes novos (primária criada/protegida, corpo pesquisável). 134 testes server verdes; fan-out completo (lint/typecheck/test) verde.
- **TASK-67 ✅** (ui-ux): editor admin. Novo endpoint `tabs.getPrimary({pageId})` (a primária fica fora de `listByPage`). `TabContentPage` virou **`PageContentPage`** (arquivo novo, TabContentPage removido): trata `/pages/:pageId` (corpo/primária) e `/pages/:pageId/tabs/:tabId` (tab de usuário) na mesma superfície; tab bar `Corpo + tabs` só aparece com ≥1 tab de usuário; Publicar/Histórico seguem no nível de página. `SidebarTree`: título da página virou NavLink p/ `/pages/:pageId` + caret separado p/ expandir tabs. Rotas em `main.tsx`: adicionada `pages/:pageId`. +1 teste server (`getPrimary`, 135 total). **Verificado via Playwright** (11 checks, server buildado porta 3217): página abre no corpo sem tab bar, autosave do corpo, tab bar aparece ao criar tab, primária nunca listada como tab de usuário, navegação Corpo↔Usage.
- **TASK-68 ✅** (ui-ux): render público. `PageRenderer` separa corpo (primária) de tabs de usuário: sem tab bar quando só há corpo; com tabs, mostra visão **"Visão geral"** (corpo, `BODY_VIEW_LABEL`) + as tabs — a primária nunca aparece com o título interno "Conteúdo". Compat com snapshots antigos (sem `isPrimary` → todas viram tabs, comportamento original). `PublicPageView` mapeia a Visão geral p/ URL **sem** `tabId` e tabs de usuário p/ `/:tabId`. Busca/PublicSidebar já linkam `/docs/:section/:page` (sem tabId) → caem no corpo. `RevisionSnapshotPreview` (admin) usa o mesmo `PageRenderer` → corpo vira "Visão geral" ali também. **Verificado via Playwright deslogado** (12 checks + 2 de reconfirmação): corpo-só sem tab bar, corpo+tab com tab bar, primária não exposta, navegação Visão geral↔tab com URL correta, deep-link direto na tab, busca do corpo leva à página. **Bloco de conteúdo na página (TASK-65–68) COMPLETO e MERGEADO na `main`** (PR #6, merge commit `523c340`, branch deletada; CI 2 jobs `build` verdes, 150 testes locais verdes antes do merge).
- **TASK-69 ✅** (functional, branch `feature/historico-geral`): histórico geral do painel. Novo `revisions.listRecent({limit≤100, default 50})` (`protectedProcedure` admin+editor — decisão registrada, editor tb gera revisões; `innerJoin pages` p/ título, `leftJoin users`, ordem `criadoEm desc` + `rowid` desc). `GlobalHistoryPage` em `/admin/history` (link no header, visível a admin+editor): feed cronológico, evento derivado da `mensagem` (`Restaurou` se começa com "Restaurado da revisão de", senão `Publicou`), cada linha linka p/ `/pages/:pageId/history`. +4 testes server (agrega multi-página, limit, autor null pós-delete, UNAUTHORIZED — 139 total). **Verificado via Playwright** (server buildado 3219): link no header, empty-state, 3 revisões de 2 páginas ordenadas com autor+link, evento "Restaurou" após restore. **MERGEADO na `main`** (PR #7, merge `ebe52ed`). **Follow-up não feito**: audit log estrutural (create/rename/delete de user/section/page/tab, tokens) exigiria tabela `audit_log` própria — fica como task futura se desejado.
- **TASK-70 ✅** (functional, branch `feature/auto-slug`): auto-slug. `slugify(titulo)` exportado de `pages.ts` (NFD + strip `̀-ͯ`, lowercase, run de não-alfanum → `-`, apara pontas). `pages.create` input `slug: slugSchema.optional()`: se ausente/branco → deriva; vazio (só emoji/símbolo) → BAD_REQUEST; **de-dup só p/ derivados** (`-2/-3…`, consulta slugs da seção dentro da tx); slug **digitado** mantém validação regex + CONFLICT na colisão (inalterado). Admin `CreatePageForm`: slug opcional (`placeholder "slug (opcional)"`, hint "Deixe em branco para gerar do título"), envia `undefined` quando branco. +9 testes server (5 unit `slugify` + 4 create: derivado/de-dup/CONFLICT digitado/BAD_REQUEST) = 148. **Verificado via Playwright** (6 checks): hint, "Get started"→get-started, 2º→get-started-2, "Botão"→botao, emoji→erro. **MERGEADO na `main`** (PR #8, merge `df2099d`).
- ⚠️ **Flake conhecido**: `pnpm test` (fan-out **paralelo**) às vezes falha o preview-kit com `(0, act) is not a function` (resolução do React sob concorrência); passa isolado e com `pnpm -r --workspace-concurrency=1 run test`. Não é regressão de código — usar a versão serializada p/ validar local; CI vem verde.
- **TASK-71** (setup): publicar `@systembook/connector` + deps no npm com `bin systembook-connector` (era a antiga TASK-65) (categoria setup): publicar `@systembook/connector` + deps `@systembook/schema`/`@systembook/preview-kit` no npm com `bin systembook-connector` — hoje os três são `private:true`/`0.0.0`/sem `bin`/`workspace:*`, então o `pnpm add`/`npx` do `docs/setup.md` ainda não funciona de fora do monorepo; a task fecha esse gap e remove as notas de pré-lançamento dos docs.

## Estado atual

**Tasks 1–68 mergeadas na `main`** (Fases 0–6, Fase 7/empacotamento via **PR #5**, e o bloco de conteúdo na página TASK-65..68 via **PR #6**). Backlog aberto: TASK-69 (histórico geral, em desenvolvimento), TASK-70 (auto-slug), TASK-71 (publicar connector no npm). Existe um `CLAUDE.md` na raiz com o guia do repositório. **Atenção à numeração real dos `.json`** (a tabela de fases herdada abaixo estava trocada): TASK-52 layout público, TASK-53 busca FTS5 (backend), TASK-54 UI de busca, **TASK-55 tema dark/light + tipografia**, TASK-56 landing customizável, TASK-57 layout responsivo.

| Task | Status | Verificação |
| --- | --- | --- |
| TASK-1..8 | ✅ | Fase 0 — ver histórico do git e seções abaixo |
| TASK-9 | ✅ | argon2id + pepper; testes de hash/verify/salt aleatório/legado scrypt |
| TASK-10 | ✅ | `auth.login` seta cookie httpOnly; erro genérico anti-enumeração testado |
| TASK-11 | ✅ | `protectedProcedure`/`adminProcedure`; sessão expirada limpa a linha no acesso |
| TASK-12 | ✅ | Tela de login verificada via Playwright (erro inline, redirect, guard) |
| TASK-13 | ✅ | `/admin/users` com lista + criação; editor vê "Acesso negado" (Playwright) |
| TASK-14 | ✅ | `users.list/create/update/deactivate` com testes de FORBIDDEN/CONFLICT/self-block |
| TASK-15 | ✅ | Reset de senha invalida sessões; senha antiga falha e nova funciona (Playwright) |
| TASK-16 | ✅ | Logout limpa cookie; UNAUTHORIZED global redireciona para /login (Playwright) |
| TASK-17 | ✅ | Tabela `sections` (migration 0001); ordem renumerada pela aplicação |
| TASK-18 | ✅ | `sections.list/create/rename/reorder/delete` com testes (reorder valida lista completa) |
| TASK-19 | ✅ | Tabela `pages` (migration 0002) com unique `(section_id, slug)` |
| TASK-20 | ✅ | `pages.*` incl. `updateSlug` (CONFLICT) e reorder escopado à seção |
| TASK-21 | ✅ | Tabela `tabs` (migration 0003); título livre, duplicata permitida (MVP) |
| TASK-22 | ✅ | `tabs.*` espelhando pages sem slug; helper compartilhado de reorder |
| TASK-23 | ✅ | SidebarTree no painel verificada via Playwright (12 cenários E2E) |
| TASK-24 | ✅ | Matriz documentada em `router.ts`; `permissions.test.ts` trava editor=CRUD estrutura, users.* FORBIDDEN |
| TASK-25 | ✅ | ContentEditor (Tiptap v3) montado na rota da tab; instância nova por tab (E2E) |
| TASK-26 | ✅ | Heading 1-3, bold/italic, listas, code block + toolbar com estado ativo (E2E) |
| TASK-27 | ✅ | Tabela 3×3 com header, +/− linha/coluna contextuais; JSON confere com schema (E2E) |
| TASK-28 | ✅ | Nó custom `callout` (info/warning/tip) com NodeView React e switcher in-place (E2E) |
| TASK-29 | ✅ | Nó atômico `componentEmbed` placeholder com attrs componentName/variantId (E2E) |
| TASK-30 | ✅ | Tabela `blocks` (migration 0004) + helpers tipados; round-trip dos 8 tipos testado |
| TASK-31 | ✅ | `serialize.ts` doc↔blocks + router `blocks.getByTab`; round-trip dos 8 tipos com marks/attrs |
| TASK-32 | ✅ | Autosave com debounce 2s, indicador Salvando…/Salvo, flush no unmount (16 checks E2E) |
| TASK-33 | ✅ | Tabela `revisions` (migration 0005) com `autor_id` SET NULL; tipo `PageSnapshot` no schema |
| TASK-34 | ✅ | `pages.publish` cria snapshot via `buildPageSnapshot`/`createRevision`; flush de tab ativa antes do publish (Playwright) |
| TASK-35 | ✅ | `/pages/:id/history` lista revisões (autor + timestamp), preview read-only por revisão (Playwright) |
| TASK-36 | ✅ | `pages.restoreRevision` transacional (skip de tabs removidas), cria revisão de acompanhamento; restore verificado via Playwright |
| TASK-37 | ✅ | `PreviewConfig` final em `packages/schema` (variants + união discriminada de controls text/boolean/select); JSDoc com contrato do `*.preview.tsx` |
| TASK-38 | ✅ | `preview-kit` `mount()` com variantes, erro para variantId desconhecida, postMessage com allow-list de origin; 7 testes vitest+jsdom |
| TASK-39 | ✅ | connector CLI `discover --root` (fast-glob + esbuild bundle + import de temp + zod); fixture com 2 válidos/1 malformado/1 em node_modules; 2 testes |
| TASK-40 | ✅ | connector `generate`: entry tsx+html por variante em `.systembook/entries/{comp}--{variant}/`, limpeza de órfãos, colisão de slug = erro; 3 testes + smoke de bundle |
| TASK-41 | ✅ | connector `build`: Vite multi-entry (base ./, configFile false, alias preview-kit/react); artefato verificado em Chromium headless sob path aninhado, postMessage ok |
| TASK-42 | ✅ | Tabela `component_previews` (migration 0006, índice composto p/ latest) + `insertComponentPreview`/`getLatestPreview` com desempate por rowid; 4 testes |
| TASK-44 | ✅ | Tabela `upload_tokens` (migration 0007) + router adminProcedure (create/list/revoke) + tela `/admin/settings/tokens` com reveal único; 6 testes + 12 checks Playwright |
| TASK-43 | ✅ | `POST /api/previews` fora do tRPC: bearer token + multipart (busboy) + extração tar filtrada; 10 testes + upload E2E real via curl |
| TASK-45 | ✅ | `docs/ci-example.md` (workflow GitHub Actions completo) + `manifest.json` no build do connector; loop de upload validado literalmente contra server real |
| TASK-46 | ✅ | `GET /previews/*` público com cache imutável + traversal barrado; 10 testes + smoke contra server buildado |
| TASK-47 | ✅ | `component-embed` renderiza iframe real do artefato mais recente via `componentPreviews.getLatest`; 5 testes + 7 checks Playwright (fluxo TASK-41→43→47 real) |
| TASK-48 | ✅ | Picker de componente/variante (`componentPreviews.listComponents`/`listVariants`) na toolbar + re-seleção sobre embed existente; 4 testes + 10 checks Playwright |
| TASK-49 | ✅ | Painel de controles (`PreviewConfig.controls`) → `postMessage systembook:update-props` ao iframe; connector grava `preview-config.json` no artefato, `getLatest` retorna a config; 3 testes + 9 checks Playwright |
| TASK-50 | ✅ | Doc pública read-only (rota `/p/:pageId`, sem auth) da última revisão publicada via `revisions.getLatestPublished`; `PageRenderer` compartilhado; `getLatest`/`getLatestPublished` viraram públicos; 2 testes + 14 checks Playwright deslogado |
| TASK-51 | ✅ | Empty-state do embed polido: estado `empty` (referenciado sem preview) âmbar/⚠️ distinto do `unset` cinza/🧩, botão "Tentar novamente" (`refetch`), reuso automático no público read-only; UI-only + 13 checks Playwright |
| TASK-52 | ✅ | Layout público navegável `/docs/:sectionSlug/:pageSlug` (fora do AdminLayout, sem auth): sidebar de seções/páginas publicadas, `sections.listPublic`/`pages.getPublishedBySlug` (públicos), slug de section (migration 0008 + backfill); 6 testes + 17 checks Playwright deslogado |
| TASK-53 | ✅ | Busca full-text FTS5: virtual table `pages_fts` (migration 0009 raw), reindex por publicação (publish/restore), `search.query` (publicProcedure) com snippet destacado; 5 testes vitest |
| TASK-54 | ✅ | UI de busca em dropdown ao vivo no header público (`SearchBox.tsx`, debounce 300ms), snippet com `<mark>`, navegação por clique/teclado, estados vazio/sem-resultado; substituiu a página `/docs/search` da TASK-53; 12 checks Playwright deslogado |
| TASK-55 | ✅ | Tema claro/escuro + tipografia da doc pública: tokens CSS por tema em `.sb-public[data-theme]`, toggle no header (`useTheme`, localStorage + prefers-color-scheme), prosa sem chrome de editor, callouts/code/tabela theme-aware; 10 checks Playwright (2 sistemas) + screenshots claro/escuro |
| TASK-56 | ✅ | Página inicial customizável na raiz pública (`/docs`), reusando tabs/blocks/revisions via section/page/tab reservadas (ids sentinela); router `landing.get`/`getEditorTarget`, rota admin `/admin/settings/landing-page`, estado padrão quando não publicada; landing oculta de nav/busca; 4 testes vitest + 12 checks Playwright (fluxo admin real) |
| TASK-57 | ✅ | Layout responsivo da doc pública (<768px): sidebar vira drawer off-canvas com hambúrguer + backdrop, busca vira gatilho-ícone → overlay full-screen, tabelas rolam no `.tableWrapper` (layout auto + nowrap só no público), alvos de toque ≥44px; UI-only; 19 checks Playwright (desktop 1440 + mobile 390) + screenshot mobile |

**Cobertura**: 141 testes vitest (126 server + 7 preview-kit + 8 connector), todos verdes via `pnpm test`. E2E Playwright/curl ad-hoc (scratchpad, não commitados): editor base (29), nós custom (12), autosave (16), árvore (12), publish/histórico/restore, tokens (12 checks), artefato de preview em Chromium headless, o loop de CI documentado contra server real, o iframe de preview do component-embed (7 checks), o picker de inserção/re-seleção (10 checks), o painel de controles interativos (9 checks), a doc pública deslogada com os 7 tipos de bloco + embed (14 checks) e os empty-states do embed (13 checks).

O tracking granular (pass por step) está em `.prd/tasks/TASK-*.json` e o índice em `.prd/tasks.json`.

### Fases da PRD × Tasks (`.prd/PRD.md` §12)

| Fase | Tasks | Status | Conteúdo |
| --- | --- | --- | --- |
| 0 — Fundamentos | TASK-1..8 | ✅ | monorepo, schema, Drizzle+SQLite, tRPC health-check, Dockerfile, compose, CI |
| 1 — Auth e painel base | TASK-9..16 | ✅ | argon2, login/cookie, middleware admin/editor, tela de login, gestão de usuários, reset de senha, logout |
| 2 — Estrutura de navegação | TASK-17..24 | ✅ | data models e CRUD de sections/pages/tabs, árvore na sidebar, permissões editor=admin |
| 3 — Editor de conteúdo | TASK-25..36 | ✅ | Tiptap + extensões + tabela + callout + component-embed placeholder, blocks, serialização, autosave, revisions, publish, histórico/restore |
| 4 — Conector e preview | TASK-37..46 | ✅ | PreviewConfig schema, preview-kit, connector CLI (discovery/entrypoints/build via Vite), component_previews, upload endpoint autenticado, tokens, exemplo CI, rota de artefatos estáticos |
| 5 — Integração do preview | TASK-47..51 | ✅ | component-embed com iframe real, seletor componente/variante, painel de controles, doc pública com embeds, empty-state "sem preview disponível" — **live preview funcional (marco central do PRD)** |
| 6 — Publicação e polimento | TASK-52..57 | ✅ | layout público (52), busca FTS5 backend (53), UI de busca dropdown (54), tema dark/light+tipografia (55), landing customizável (56), layout responsivo (57) — **mergeada (PR #4)** |
| 7 — Empacotamento e lançamento | TASK-58..64 | ✅ | imagem Docker publicada, compose de produção, docs de setup/CI/schema, README, CONTRIBUTING+licença, docs de backup — **mergeada (PR #5)** |
| Pós-fases — Conteúdo na página | TASK-65..68 | ✅ | página com corpo próprio + tabs opcionais (data-model/pipeline/editor/público) — **mergeada (PR #6)** |
| Pós-fases — Histórico geral | TASK-69 | ✅ | `/admin/history` agregando revisões de todas as páginas — **mergeada (PR #7)** |
| Pós-fases — Auto-slug | TASK-70 | ✅ | slug derivado do título quando em branco — **mergeada (PR #8)** |
| 8 — Bloco Dos and Don'ts | TASK-71..74 | ✅ | schema+serialize (71), NodeView+toolbar (72), cover opcional imagem/component-embed (73), render público+verificação (74) — **mergeada (PR #9)** |
| 9 — Modernização visual (Shadcn) | TASK-75..81 | ✅ | setup Tailwind+shadcn (75), lucide-react+sonner (76), auth/shell (77), sidebar (78), editor (79), telas de gestão (80), QA final (81) — escopo admin-only, doc pública mantém o tema CSS-vars da TASK-55; **mergeada (PR #10)** |
| 10 — Redesign de interface (Zeroheight) | TASK-82..93 | ✅ | tokens visuais (82), **entidade Menu nova acima de Seção**: schema+migration com backfill (83), CRUD tRPC (84), header com nav de menus (85), sidebar 2 níveis escopada ao menu ativo (86), área principal/Section Header+breadcrumbs c/ Menu (87), toolbar de blocos (88), menu contextual da sidebar (89), empty states incl. menu vazio (90), busca admin incl. menus (91), responsivo (92), QA final (93) — **todas as 12 tasks concluídas e verificadas; ainda NÃO mergeada na `main`** (branch `fase-10-tokens-visuais`); ver `plano-de-interface.md` e decisões abaixo |
| 10 — Polimento visual pós-QA (Zeroheight) | TASK-94..95 | ✅ | acabamento pedido pelo usuário comparando `referencia-3.png` (CMS) × `referencia.png` (Zeroheight): **create-actions viram text-links de acento (94) ✅** (`createLinkClass` em `lib/styles.ts`) e **chrome secundário achatado (95) ✅** (reset `.sb-admin button{background:transparent}` no `@layer base` — resolve o native `ButtonFace` fill de raiz, sem preflight; + search trigger borderless). Fills intencionais preservados (bg-accent ativo, bg-primary Publicar, kbd). Fase 10 completa; falta só PR + merge |
| Backlog aberto (fora das fases) | TASK-96 | ⬜ | publicar `@systembook/connector` no npm (era TASK-71 → TASK-82 → TASK-92 → TASK-94 → TASK-96, renumerada 4x — sempre a última, agora após as TASK-94/95 de polimento) |

Critérios de sucesso do PRD (§1): fim da Fase 3 = CMS de texto utilizável sem dev (✅ atingido); fim da Fase 5 = live preview funcional (proposta de valor central); fim da Fase 7 = pronto para divulgação open source.

## Git / CI

- Histórico inicial: **14 commits Conventional Commits** na `main` (chore/build/feat/test/ci), pushados para `github.com/mateusvillain/systembook`.
- CI (GitHub Actions): Node 24, pnpm via campo `packageManager`, steps separados Lint → Typecheck → Test. **Gotcha corrigido**: `pnpm/action-setup@v4` não aceita `version:` no workflow quando `package.json` tem `packageManager` — deixar só o `packageManager`.
- Branch de teste `ci-test/lint-failure` foi criado e removido (local + remoto) após validar a falha seletiva do lint.
- **PRs mergeados**: #1 (Fase 4), #2 (Fase 5), #3 (fix proxy Vite dev), **#4 (Fase 6 — TASK-52..57)**, **#5 (Fase 7 — TASK-58..64, merge commit `c614faa`)**, **#6 (conteúdo na página — TASK-65..68, merge commit `523c340`)**, **#7 (histórico geral do painel — TASK-69, merge commit `ebe52ed`)**, **#8 (auto-slug — TASK-70, merge commit `df2099d`)**, **#9 (Fase 8 — bloco Dos and Don'ts, TASK-71..74, merge commit `6b61734`)**, **#10 (Fase 9 — modernização visual Shadcn, TASK-75..81, merge commit `8656c9a`)**. Cada fase/feature fecha com PR + merge na `main` e branch deletada; o CI (2 jobs `build`) precisa estar verde antes do merge. Validação da Fase 7 antes do merge: `pnpm lint/typecheck/test` local verde (146 testes: 131 server + 7 preview-kit + 8 connector) + os 2 jobs `build` do CI verdes.

## Arquitetura de auth (Fase 1)

- **Hashing**: `apps/server/src/auth/password.ts` — argon2id (memoryCost 19456, timeCost 2) com pepper de `ARGON2_SECRET` concatenado antes do salt. `verifyPassword` também aceita hashes legados `scrypt$...` (do seed pré-TASK-9) e `needsRehash` sinaliza; o login re-hasheia transparentemente para argon2id.
- **Sessões**: cookie `session_id` httpOnly/SameSite=Lax/Path=/ (Secure em produção), 7 dias, linha em `sessions`. Cleanup preguiçoso: sessão expirada é deletada no acesso (sem cron).
- **Contexto tRPC**: `src/trpc/context.ts` resolve `{ userId, role, sessionId }` do cookie; `init.ts` expõe `protectedProcedure` (UNAUTHORIZED) e `adminProcedure` (FORBIDDEN) — ponto único de enforcement para todas as fases futuras.
- **Routers**: `auth` (login com erro genérico anti-enumeração, logout, me), `users` (list/create/update/deactivate/resetPassword, todos adminProcedure). Reset de senha deleta todas as sessões do alvo.
- **Decisão deactivate = hard delete** (sessions/memberships caem por FK cascade). ⚠️ Quando a tabela `revisions` for criada (Fase 3), `autor_id` deve ser nullable ou `ON DELETE SET NULL` para não quebrar com usuários removidos.
- **Admin panel**: React Router 7 (`/login`, `/` protegida, `/admin/users`), tRPC client via `@trpc/tanstack-react-query` (`createTRPCContext` → `TRPCProvider`/`useTRPC`), QueryCache/MutationCache global redirecionando UNAUTHORIZED → `/login`. `AppRouter` type importado de `@systembook/server` via `exports.types` apontando ao fonte (`src/trpc/router.ts`).

## Estrutura de navegação (Fase 2)

- **Tabelas**: `sections (id, titulo, ordem)`, `pages (id, section_id, titulo, slug, ordem)` com unique `(section_id, slug)`, `tabs (id, page_id, titulo, ordem)` — migrations 0001–0003. FK cascade em toda a árvore; deletar seção leva pages/tabs (e blocks/revisions futuros).
- **`ordem`**: inteiro renumerado pela aplicação a cada reorder (0..n-1 dentro do pai), sem unique index; leitura ordena por `(ordem, id)` para desempate. `reorder` exige a lista **completa** de ids do pai (parcial/repetida/estranha → BAD_REQUEST) — validação no helper compartilhado `src/trpc/routers/reorder.ts`.
- **Permissões**: sections/pages/tabs usam `protectedProcedure` (admin E editor, decisão TASK-24 do PRD); users continua `adminProcedure`. `isUniqueViolation` foi extraído para `src/db/errors.ts`.
- **Slug**: `slugSchema` (`^[a-z0-9]+(-[a-z0-9]+)*$`) exportado de `pages.ts`, compartilhado entre `create` e `updateSlug`; violação de unique vira CONFLICT.
- **Admin**: `SidebarTree` (`apps/admin/src/features/navigation/`) na lateral do `AdminLayout` — colapsável por nível, criar/renomear inline, reorder por botões ↑/↓ (drag-and-drop adiado), exclusão via `window.confirm` avisando do cascade. Tab clicada navega para `/pages/:pageId/tabs/:tabId` (`TabContentPage`, placeholder até a Fase 3). Pages/tabs são buscadas lazy ao expandir o pai (`listBySection`/`listByPage` por nó).

## Editor Tiptap (Fase 3, TASK-25/26/27)

- **Tiptap v3 (3.28)** em `apps/admin/src/features/editor/` — `ContentEditor.tsx` + `EditorToolbar.tsx` + `editor.css`. Montado em `TabContentPage` com `key={tabId}` (instância nova por tab; conteúdo ainda **não persiste** — autosave/blocks nas TASK-31/32).
- **Sem StarterKit de propósito** (nota da TASK-26): set intencional de extensões espelhando os block types do PRD — Document/Paragraph/Text, Heading (níveis 1–3), Bold, Italic, BulletList/OrderedList/ListItem (pacote consolidado `@tiptap/extension-list` no v3), CodeBlock, Table/TableRow/TableHeader/TableCell (`@tiptap/extension-table`, `resizable`), UndoRedo/Dropcursor/Gapcursor (`@tiptap/extensions`). Strike, blockquote, HR etc. ficam fora até o schema de blocks mudar.
- **Toolbar**: estados ativos via `useEditorState` — no Tiptap v3 `useEditor` **não** re-renderiza a cada transação por padrão. Botões usam `onMouseDown={preventDefault}` para não roubar a seleção. Controles de tabela (±linha/±coluna) só aparecem com o cursor dentro de uma tabela.
- A instância ativa é exposta em `window.systembookEditor` (para E2E/automação — ex.: `getJSON()`).
- **Gotcha de E2E headless**: a sync da seleção nativa do browser → estado ProseMirror é assíncrona; dblclick/Shift+setas não refletem em `state.selection` a tempo. Para testar atalhos sobre seleção, criar a seleção com `setTextSelection` programático e então mandar o atalho de teclado.
- JSON de tabela confirmado contra `TableBlockContent` do schema (cabe em `body: TiptapJson`); forma documentada em `packages/schema/src/block.ts`.

### Nós custom (TASK-28/29)

- `nodes/Callout.tsx` e `nodes/ComponentEmbed.tsx` — padrão estabelecido: extensão `Node.create` + NodeView React (`ReactNodeViewRenderer`/`NodeViewWrapper`/`NodeViewContent`) no mesmo arquivo, attrs espelhados em `data-*` no parse/renderHTML. Este é o modelo para nós futuros (preview real na TASK-47).
- **Callout**: `content: 'block+'`, attr `variant` (info/warning/tip, arrays/meta exportados como `CALLOUT_VARIANTS`/`CALLOUT_META`); toolbar tem picker de 3 botões (info primeiro = default) e o NodeView tem switcher in-place via `updateAttributes` (preserva conteúdo). **Gotcha**: `:focus-within` não funciona para mostrar controles em NodeView — o foco fica no host `.ProseMirror`, não no nó; o switcher fica sempre visível com opacity baixa + hover.
- **ComponentEmbed**: `atom: true`, attrs `componentName: ''`/`variantId: null`; placeholder tracejado até TASK-47/48. Clicar gera NodeSelection (em asserts E2E usar `selection.node?.type.name`, nunca `constructor.name` — o build minificado renomeia classes).
- Nota: o schema permite `componentEmbed`/`callout` aninhados dentro de callout (`block+`) — aceito no MVP; o `focus('end')` dentro de um callout insere aninhado, cuidado em scripts.

### Persistência e autosave (TASK-31/32/33)

- **Serialização** em `apps/server/src/blocks/serialize.ts` (funções puras, server não depende de @tiptap/*): `tiptapDocToBlocks` mapeia cada nó top-level para uma linha (`bulletList`/`orderedList` → tipo `list`; `codeBlock` → `code` estruturado `{language, code}`; `componentEmbed` → attrs estruturados; heading/paragraph/callout guardam `content` em `body`; list/table guardam o **nó completo** em `body` para preservar attrs como `start`/`colwidth`). `blocksToTiptapDoc` inverte ordenando por `ordem`. Nó desconhecido → `UnknownNodeTypeError` → BAD_REQUEST no router. **Gotcha TS**: `TiptapNode`/`TiptapDoc` são type aliases, não interfaces — interfaces não são atribuíveis ao shape com index signature inferido pelo zod (`z.looseObject`).
- **Router `blocks`**: `getByTab` (retorna `{doc, blocks}`; `doc: null` se a tab nunca foi salva) e `saveDraft` (delete+insert transacional via `replaceBlocksForTab`; **nunca** toca revisions — separação autosave × publish testada). Ambos protectedProcedure, matriz em router.ts atualizada.
- **Autosave no admin** (`ContentEditor.tsx`): outer component carrega `getByTab` com `gcTime: 0` (sem cache entre montagens — o flush do unmount anterior pode ter mudado o rascunho) e monta `EditorInner` com o doc inicial; `onUpdate` → debounce 2s → `saveDraft`; indicador `data-save-status` (saving/saved/idle/error, aria-live); flush fire-and-forget no unmount quando há debounce pendente. Refs (`pendingDocRef`/`flushRef`) evitam closures velhas dentro do `useEditor` (deps `[tabId]`).
- **Gotcha de NodeView + caret**: ao inserir callout com o cursor num parágrafo não-vazio, o `setTextSelection` muda o estado PM mas o **caret do DOM não entra** no NodeView React (monta assíncrono; o selectionToDOM não reposiciona depois) — a digitação segue o caret e cai fora do nó. Solução na toolbar: após inserir, posicionar `Range` do DOM manualmente via `view.domAtPos` com retry em rAF até o contentDOM existir (`insertCallout` em `EditorToolbar.tsx`).
- **revisions** (migration 0005): `id, page_id FK cascade, snapshot_json, autor_id FK SET NULL (nullable!), criado_em, mensagem?`. Desvio deliberado do esboço da task (que dizia notNull): usuários sofrem hard delete e a revisão sobrevive com autor null — decisão antiga da TASK-14 aplicada. `snapshot_json` = `PageSnapshot` de @systembook/schema (página inteira: todas as tabs + blocks), pois Publicar é ação de página.
- **`pages.publish` (TASK-34)**: único ponto de escrita em `revisions` (helpers `buildPageSnapshot`/`createRevision` em `src/db/revisions.ts`). Monta o snapshot lendo `tabs` + `listBlocksByTab` por tab; `BlockRecord` (`tipo`/`conteudo`) é remapeado para `Block` (`type`/`content`) com um cast — os literais batem 1:1, só o nome dos campos muda. `autorId` vem de `ctx.user.userId`, nunca do input. Página inexistente → NOT_FOUND.
- **Admin**: botão "Publicar" vive em `TabContentPage.tsx` (nível de página, não de tab) — `ContentEditor` virou `forwardRef<ContentEditorHandle>` expondo `flush(): Promise<void>` (a antiga função fire-and-forget do autosave virou `mutateAsync` aguardável); `handlePublish` faz `await editorRef.current?.flush()` antes de `pages.publish.mutate` para não snapshotar um rascunho desatualizado da tab ativa (as outras tabs já persistiram via autosave ao trocar de rota). Feedback via banner `role="status"`/`role="alert"` (mesmo padrão do `UsersPage`).
- **Verificado via Playwright** (script ad-hoc no scratchpad, server buildado servindo `apps/admin/dist` na porta 3210): editar 2 tabs, digitar mais uma vez sem esperar o debounce de 2s e clicar Publicar imediatamente — a revisão criada contém a edição de última hora (confirma o guard de flush do step 3). Só 1 revisão por publish, `autor_id` correto.
- Pendência real para a TASK-50 (documentada no spec da TASK-34): o read path público deve ler da última revisão, não de `blocks` ao vivo — ordering dependency entre as duas tasks.

### Histórico de revisões + restore (TASK-35/36)

- **TASK-36 (restore) foi implementada junto com a TASK-35**: a UI de histórico não dava para verificar em browser sem um endpoint de restore funcional, então adiantei o `pages.restoreRevision` mesmo a TASK-35 listando só TASK-34 como dependência formal.
- **`db/revisions.ts`**: `restoreRevision(db, { pageId, targetRevision, autorId })` roda tudo numa única transação — substitui os blocks de cada tab ainda existente (via `replaceBlocksForTabInTx`, nova função extraída de `replaceBlocksForTab` para não aninhar `db.transaction()`, que o better-sqlite3/drizzle não suporta) e, ao final, cria uma revisão de acompanhamento lendo o estado já restaurado (`buildPageSnapshot(tx, pageId)` dentro da mesma tx) com `mensagem: "Restaurado da revisão de {ISO date}"`. Histórico continua append-only — restore nunca reescreve revisões antigas.
- **Tab drift**: tabs do snapshot alvo que não existem mais na página são puladas (não é erro) e retornadas em `skippedTabIds` — decisão MVP documentada no próprio código.
- **`Db`/`DbTx`** (novo tipo em `db/client.ts`): `DbTx = Parameters<Parameters<Db['transaction']>[0]>[0]` — usado sempre que uma função precisa rodar dentro de uma transação já aberta por quem chama (ex.: `listBlocksByTab`, `buildPageSnapshot`, `replaceBlocksForTabInTx` aceitam `Db | DbTx`).
- **Router `revisions`** (novo, `src/trpc/routers/revisions.ts`): `listByPage` (leftJoin com `users` para o email do autor — `autor_id` pode ser null; **desempate de ordenação por `sql`${revisions}.rowid`` além de `criadoEm` desc**, porque `criadoEm` só tem resolução de segundo e publishes rápidos em sequência empatavam) e `getById` (snapshot completo parseado). `pages.restoreRevision` ficou em `pages.ts` (mesmo router do `publish`), não em `revisions.ts`, seguindo o texto literal do spec da TASK-36.
- **Admin**: `editorExtensions` foi extraído de `ContentEditor.tsx` para `features/editor/extensions.ts` (single source of truth do conjunto de nodes/marks), reaproveitado pelo preview read-only de revisões. `features/revisions/blocksToTiptapDoc.ts` **duplica** deliberadamente o mapeamento block→nó de `apps/server/src/blocks/serialize.ts` — não dá pra importar direto do server (fronteira de pacote/runtime, mesma razão do `@systembook/schema` ser types-only); qualquer mudança no mapeamento canônico (`packages/schema/src/block.ts`) precisa espelhar nos dois lugares.
- **Gotcha de tipos tRPC v11 sem transformer**: o output inferido pelo client não é o tipo de domínio puro (`PageSnapshot`, `Block`) — o tRPC aplica um `Serialize<T>` interno que marca campos `unknown`/possivelmente-undefined como opcionais e (provavelmente) `Date` como string. Componentes que recebem dados de query devem tipar a partir de `RouterOutput` (novo export em `apps/admin/src/lib/trpc.ts`, via `inferRouterOutputs<AppRouter>` — precisou adicionar `@trpc/server` como devDependency do admin só para o tipo), não do tipo de domínio importado de `@systembook/schema`; um cast (`as Block[]`) é necessário na borda onde o valor "solto" do wire é passado para uma função que espera o tipo de domínio exato.
- **Rota**: `/pages/:pageId/history` (não `/admin/pages/:pageId/history` como o texto do spec sugeria) — decisão deliberada para consistência com a rota irmã `/pages/:pageId/tabs/:tabId` (o prefixo `/admin/` no código existente é reservado à área de gestão de usuários, `admin/users`). Link "Histórico" adicionado ao lado do botão Publicar em `TabContentPage`.
- **Verificado via Playwright**: publicar v1, editar e publicar v2, abrir `/pages/:id/history`, ver as 2 revisões (mais recente primeiro, com email do autor e timestamp), selecionar a mais antiga → preview read-only mostra só o conteúdo da v1, clicar Restaurar → `window.confirm` → navega de volta para `/pages/:id/tabs/:tabId` → editor mostra o conteúdo restaurado da v1.

### Modelo de blocks (TASK-30)

- Tabela `blocks (id, tab_id FK cascade, tipo, conteudo_json, ordem)` — migration 0004. `tipo` validado **na aplicação** (decisão TASK-30): array `BLOCK_TYPES` em `schema.ts` com `satisfies readonly BlockType[]` + assert de exaustividade nas duas direções, e zod enum `blockTypeSchema` em `src/db/blocks.ts`; sem CHECK no SQLite (crescer o set pós-MVP não exige migration). O array de runtime vive no server porque `@systembook/schema` é types-only.
- Helpers tipados em `src/db/blocks.ts`: `insertBlock`/`listBlocksByTab` fazem stringify/parse de `conteudo_json`; `BlockRecord` é union discriminada por `tipo` com `conteudo` tipado via `Extract<Block, { type: T }>['content']`. Código chamador nunca vê JSON cru.

## O que existe hoje

- **Monorepo**: `apps/server`, `apps/admin`, `packages/schema` (types-only), `packages/preview-kit` (harness mount+postMessage) e `packages/connector` (CLI discover/generate/build). Scripts raiz `build/dev/lint/typecheck/test` fazem fan-out com `pnpm -r --if-present`. Docs para times consumidores em `docs/` (por ora `ci-example.md`).
- **Server** (`apps/server`): Node http nativo + tRPC v11 montado em `/trpc`; valida env fail-fast (`src/env.ts`); roda migrations no boot (`src/db/migrate.ts`); seed de bootstrap idempotente (`src/db/seed.ts`); serve o build estático do admin (`src/static.ts`, princípio do container único). Dev: `pnpm dev` (tsx watch com `--env-file-if-exists=../../.env.local`).
- **Banco**: better-sqlite3 + Drizzle. Tabelas `users`, `sessions`, `memberships` (migration `drizzle/0000_*.sql`). IDs são UUID. Banco local de dev em `apps/server/data/systembook.db` — admin de bootstrap já criado nele (e também no volume docker `systembook_sqlite-data`).
- **Admin** (`apps/admin`): Vite + React 19 com login, layout protegido com nav + "Sair", dashboard placeholder e gestão de usuários (criar, trocar role, remover, redefinir senha). Proxy `/trpc` no dev server.
- **Testes**: vitest em `apps/server` — `seed.test.ts`, `auth/password.test.ts`, `trpc/auth.test.ts`, `trpc/structure.test.ts` (40 testes). E2E: Playwright (root devDep) com script ad-hoc contra server em porta 3210 + banco temporário e credenciais de teste.
- **Infra**: `Dockerfile` multi-stage (imagem `systembook:dev` buildada e smoke-testada), `docker-compose.yml` de dev (verificado com ciclo up/down), CI verde.

## Fluxo de desenvolvimento local

Dois processos em dev:

- `pnpm dev` (raiz) → server (`tsx watch`, porta 3000): API tRPC, banco, sessões.
- `pnpm --filter @systembook/admin dev` → frontend com Vite na porta 5173, hot-reload dos componentes React.

O painel em dev acessa-se por `http://localhost:5173`; o proxy do `vite.config.ts` repassa `/trpc` para o server na 3000, mantendo tudo same-origin (cookies de sessão funcionam sem CORS). Sem o Vite rodando, `http://localhost:3000` também serve o painel, mas é o build estático de `apps/admin/dist` (último `pnpm build`, sem hot-reload) — exatamente o modo de produção do container único.

## Decisões técnicas tomadas

- **better-sqlite3** (não libsql) como driver — exigiu `pnpm.onlyBuiltDependencies` no `package.json` raiz (pnpm 10 bloqueia build scripts nativos por padrão).
- O seed pré-TASK-9 usava hash provisório scrypt (`scrypt$<salt>$<hash>`); desde a TASK-9 o hashing é argon2id e hashes legados são re-hasheados no primeiro login (ver "Arquitetura de auth").
- **`@systembook/schema` é types-only**: `exports` aponta direto para `src/index.ts` (sem build/dist) — zero dependência de runtime, exigência para o connector publicável.
- **`pnpm deploy` precisa de `--legacy`** no pnpm 10 (sem workspace injection) — ok porque a única dep de workspace do server é types-only.
- **ESLint 9 flat config** único na raiz (`eslint.config.mjs`); cada pacote roda `eslint src`. Regra `no-unused-vars` ignora prefixo `_`.
- **NodeNext** no server/packages (imports relativos com extensão `.js`); `bundler` no admin (Vite).
- No driver síncrono do better-sqlite3, `insert().returning()` precisa de `.get()`/`.all()` — destructuring direto do builder não compila.
- CI usa `pnpm -r --if-present run <script>` porque nem todo pacote tem todos os scripts.
- No compose de dev, `environment:` sobrepõe `env_file:` — usado para apontar `DATABASE_PATH` ao volume `/data` dentro do container.
- **zod 4** no server: usar `z.email()` (não `z.string().email()`, deprecado).
- Transações do better-sqlite3 são **estritamente síncronas** — `hashPassword` (async) precisa rodar antes de `db.transaction()`.
- O pepper em `password.ts` é lido lazy (cache no primeiro uso), não no module load — necessário para os testes que setam `ARGON2_SECRET` em `beforeEach`; `_resetPepperCacheForTests()` isola casos.
- E2E: playwright instalado como devDep da raiz + `playwright install chromium`; scripts ad-hoc no scratchpad importam playwright por caminho absoluto do node_modules.

## Fase 4 — Conector e preview (em andamento, branch `feature/fase-4-conector-preview`)

- **TASK-37 (PreviewConfig)**: tipo final em `packages/schema/src/preview-config.ts` substituiu o placeholder da TASK-3. `PreviewVariant {id, label, props}`, `PreviewControl` = união discriminada por `kind` (`text`/`boolean`/`select`, cada um com `propName`, `label?` e `defaultValue?` tipado; `select` tem `options: string[]`). Contrato do `*.preview.tsx` documentado em JSDoc: default export `satisfies PreviewConfig` + export nomeado `Preview(props)` que o harness monta. Nenhum outro código importava o placeholder ainda (o `componentEmbed` da TASK-29 não usa o tipo).
- **TASK-38 (preview-kit mount)**: `packages/preview-kit/src/mount.tsx` — `mount(rootElement, config, Component, {variantId, allowedOrigin?})` retorna `PreviewHandle {unmount}`. React/react-dom são **peerDependencies** (^19) — o pacote é bundlado pelo connector no artefato do time (TASK-41), que traz o próprio React; devDeps duplicam para os testes. `@systembook/schema` deixou de ser types-only *no consumo*: o contrato postMessage vive em `packages/schema/src/preview-messages.ts` (`PreviewUpdatePropsMessage {type: 'systembook:update-props', props}`, união `PreviewMessage`) — só tipos, o literal de runtime `UPDATE_PROPS_MESSAGE_TYPE` é exportado pelo preview-kit (mesma razão do `BLOCK_TYPES` viver no server). O painel de controles da TASK-49 deve importar o tipo do schema e o literal… de onde fizer sentido (admin não depende de preview-kit; repetir o literal com o tipo anotado é aceitável).
- **Validação de origin no mount**: `allowedOrigin` explícita > origin do `document.referrer` > `window.location.origin`. Mensagem de origin errada → `console.warn` e ignora; shape estranho → ignora em silêncio (sem warn — pode ser message de outra lib no iframe).
- **Gotchas de teste (vitest+jsdom+React 19)**: `// @vitest-environment jsdom` no docblock dispensa vitest.config; `(globalThis).IS_REACT_ACT_ENVIRONMENT = true` + `act` importado de `react` (não react-dom/test-utils); `MessageEvent('message', {origin})` aceita origin sintética no jsdom. O `exports` do preview-kit ganhou `"default": "./src/index.ts"` (fonte TS direto — o consumidor bundla; ok para Vite do connector e vitest, **não** ok para Node puro).
- **TASK-39 (connector discover)**: `packages/connector/src/discover.ts` — `discoverPreviews({root})` retorna `{previews: {filePath, config}[], failures: {filePath, message}[]}`. Pipeline por arquivo: esbuild `build` (bundle, esm, platform node, jsx automatic, write false) → grava em `mkdtemp` → import nativo → valida com zod. Falha em um arquivo não aborta os demais; CLI (`src/cli.ts`, commander) imprime ✓/✗ + resumo e `process.exitCode = 1` se houver falha. Rodar via `pnpm --filter @systembook/connector cli discover --root <dir>` (script tsx; campo `bin` fica para a fase de empacotamento, TASK-58+).
- **Espelho zod do PreviewConfig** em `connector/src/preview-config-schema.ts` com anotação `z.ZodType<PreviewConfig>` — trava em compile-time a deriva entre o validador e o tipo canônico do schema (mesmo padrão do `BLOCK_TYPES` no server: runtime fora do pacote types-only).
- **Gotcha vitest × import dinâmico** (resolvido em definitivo ao rodar o CI): vite-node reescreve `import()` e o resolver dele não acha arquivos temporários fora da raiz do projeto (`Cannot find module .../T/...`). A primeira solução (`new Function('specifier','return import(specifier)')`) funcionava no macOS/Node 25 mas **quebrava sob vitest no Linux/Node 24** com `A dynamic import callback was not specified` — todos os previews viravam `failures`, derrubando 4 testes do connector só no CI. Solução final: esbuild com `format: 'cjs'` + `createRequire(import.meta.url)` sobre o `.cjs` temporário — o require nativo é opaco aos transforms e se comporta igual sob tsx, vitest e Node puro.
- **Lição de processo**: a branch acumulou 10 commits sem push, então o CI só rodou no fim e a falha (ambiente-dependente) só apareceu no PR. Vale pushar cedo. Para reproduzir localmente uma falha só-de-CI: `git clone` da branch num dir temporário e `docker run --rm -v "$PWD:/w" -w /w node:24` rodando `pnpm install --frozen-lockfile && pnpm -r run test`.
- **Fixtures do connector** em `packages/connector/fixtures/sample-repo/` (fora de `src` — não entram em tsc/eslint): `button`/`card` válidos (com JSX; react é devDep do connector para o jsx-runtime resolver), `broken` sem `variants`, e `node_modules/fake-lib/ignored.preview.tsx` provando a exclusão — exigiu exceção no `.gitignore` raiz (`!packages/connector/fixtures/**/node_modules/`).
- Módulos dos times são executados **no processo do connector** (não sandbox) — decisão documentada no código: o connector roda no CI do próprio time, sobre o código dele mesmo; `node:vm` não seria fronteira de segurança real.
- **TASK-40 (generate)**: `connector/src/generate.ts` — `generateEntries(previews, {root, outDir?})` escreve `{outDir}/{slug(component)}--{slug(variantId)}/index.tsx + index.html` (default `<root>/.systembook/entries`, adicionado ao .gitignore raiz). **Convenção fixada**: a entry importa `config` (default) e `{ Preview }` (nomeado) do próprio `*.preview.tsx` — o contrato da TASK-37 já exigia o export `Preview`, então não há import separado do componente. `rm -rf` do outDir antes de gerar (idempotência/órfãos); colisão de slug entre previews → Error com os dois filePaths. CLI `generate --root --out-dir` roda discover+generate, exit 1 se houver preview inválido (mas gera os válidos).
- **Smoke de build validado manualmente**: entry gerada em `fixtures/sample-repo/.systembook/entries` bundla com `esbuild --bundle --jsx=automatic` (resolução: entry → preview.tsx relativo; `@systembook/preview-kit` e react resolvem subindo até `packages/connector/node_modules` — por isso preview-kit é **dependency** do connector). Base pronta para o build Vite da TASK-41.
- **TASK-41 (build)**: `connector/src/build.ts` — `buildEntries(entries, {root, outDir?})` roda `vite build` programático multi-entry (input = index.html por variante, vite root = dir de entries, outDir default `<root>/.systembook/dist`, `emptyOutDir`). Config crítica: `configFile: false` (não herdar vite.config do repo alvo), `base: './'` (artefato re-hospedado em path aninhado na TASK-46 — verificado servindo sob `/previews/x/y/`), `mode: 'production'` (sob vitest, NODE_ENV=test faria o plugin-react emitir jsx-dev-runtime). Erro de compilação rejeita o build inteiro (sem artefato parcial); CLI `build --root --out` encadeia discover→generate→build, exit 1 se houver preview inválido.
- **Gotcha dedupe do plugin-react**: `@vitejs/plugin-react` injeta `resolve.dedupe: ['react','react-dom']` automaticamente, e dedupe re-resolve a partir do **root do Vite** (o dir de entries), onde a cadeia node_modules não alcança → "Rollup failed to resolve react-dom/client". Fix: alias explícito de `react`/`react-dom` para os diretórios resolvidos **a partir do root do repo alvo** (`createRequire(root+'/x.js').resolve('react/package.json')`) — semanticamente correto (preview usa o React do time; dois Reacts quebram hooks) e a chave 'react' não engole 'react-dom' (prefixo de alias só casa seguido de '/'). `@systembook/preview-kit` também é alias, resolvido a partir do próprio connector (pnpm estrito não expõe dep transitiva).
- **Gotcha realpath no macOS**: `mkdtemp(tmpdir())` retorna path com symlink (`/var` → `/private/var`) e o rollup realpatha importers — o `../` relativo das entries erra por um nível. Testes fazem `realpath()` no workdir.
- **Verificação browser (script ad-hoc `verify-artifact.mjs` no scratchpad)**: artefato servido por http server node sob prefixo aninhado; Chromium headless confirma as 3 variantes renderizando (texto/disabled corretos), postMessage `systembook:update-props` re-renderizando no bundle real, zero console errors e zero 404.
- **TASK-44 (upload tokens)**: migration 0007 — `upload_tokens (id, token_hash unique, label, criado_em, revogado_em nullable)`. `src/auth/uploadTokens.ts`: `generateUploadToken` (32 bytes base64url), `hashUploadToken` (**SHA-256, não argon2** — token aleatório de alta entropia não precisa de KDF, nota do spec) e `findActiveUploadToken(db, raw)` (hash + `revogado_em IS NULL`) **pronto para o handler da TASK-43 consumir**. Router `uploadTokens` (create/list/revoke) todo adminProcedure — token dá escrita externa via CI, editor não gerencia; matriz em router.ts atualizada. `create` devolve o token em claro **só nessa resposta**; `list` nunca expõe hash; `revoke` é soft (seta `revogado_em`), idempotente (não move o timestamp) e NOT_FOUND para id desconhecido. Admin: `/admin/settings/tokens` (link "Tokens" no header, admin-only) com form de label, reveal único (`data-token-reveal`/`data-token-value`, aviso + botão Copiar via clipboard API) e tabela com Revogar (window.confirm). Playwright (12 checks): reveal único, token some do DOM e não volta após reload, validação aceita token ativo e rejeita revogado.
- **TASK-43 (POST /api/previews)**: `src/previews/upload.ts` — handler HTTP puro roteado em `index.ts` antes do fallback estático (**fora do tRPC** de propósito: multipart/binário + auth por bearer token de CI, não sessão). Fluxo: auth primeiro (`findActiveUploadToken`, 401) → multipart via busboy (campos `component_name`/`variant_id`/`commit_sha` + arquivo `artifact` tar.gz, streamed para temp — nunca em memória; `limits.fileSize` trunca → 400) → validação de segmento (`/^[A-Za-z0-9][A-Za-z0-9._-]{0,99}$/`, sem separadores/ponto inicial; + defesa em profundidade com resolve/startsWith) → `rm -rf` do destino e extração `tar.extract({strict, filter: só File/Directory})` (symlink/hardlink do archive são filtrados — testado) → `insertComponentPreview` com `path_estatico` **relativo** ao root (`comp/variant/sha`). Re-upload do mesmo trio sobrescreve o path e adiciona linha nova (append-only).
- **Env nova `PREVIEWS_PATH`** (opcional): default `<dir do DATABASE_PATH>/previews` — no container cai no volume /data. Adicionada em `env.ts`.
- **Nota para TASK-45**: tar criado no macOS (BSD tar) inclui arquivos AppleDouble `._*` no artefato — inofensivos, mas o doc do step de CI deve usar GNU tar (Linux) ou `COPYFILE_DISABLE=1`.
- **Gotcha de tipos do tar v7**: o callback `filter` recebe `Stats | ReadEntry`; `entry.type` só existe em ReadEntry — narrowing com `'type' in entry` (extração de archive sempre entrega ReadEntry). vitest não typechecka — o erro só apareceu no `tsc -p tsconfig.build`.
- **TASK-45 (doc de CI)**: `docs/ci-example.md` — prosa + workflow GitHub Actions copy-paste (`systembook-preview.yml`): checkout → setup-node 22 → install → `npx systembook-connector build --root .` → loop de upload por variante lendo o **`manifest.json`** que o build agora escreve na raiz do dist (`[{component, variantId, entryDir}]` — mudança de código na TASK-45 porque o nome do diretório é slug e o CI precisa do par canônico). Token via `secrets.SYSTEMBOOK_UPLOAD_TOKEN` + URL via `vars.SYSTEMBOOK_INSTANCE_URL`; tar de cada variante inclui `entryDir` + `assets/` compartilhado; `curl --fail-with-body` derruba o job em erro. **Validado literalmente**: o mesmo loop (jq+tar+curl) rodou contra server real e publicou as 3 variantes da fixture (201, linhas e volume corretos).
- **Convenção de layout do artefato** (importa para a TASK-46/47): extraído em `<previews>/<comp>/<variant>/<sha>/`, o entry HTML fica em `<entryDir>/index.html` (ex.: `button--primary/index.html`) com assets em `assets/` ao lado — a rota da TASK-46 pode localizar o entry procurando o único `*/index.html` do artefato ou usando o slug.
- **Cross-links de docs (resolvidos, TASK-60/61/62 ✅)**: `docs/setup.md` ↔ `docs/ci-example.md` ↔ `docs/preview-tsx-schema.md` e o `README.md` estão todos cross-linkados; verificado que todos os links relativos resolvem. O quick-start inline do README foi mantido como resumo, com link para o `docs/setup.md` completo no topo.
- **TASK-46 (rota de artefatos estáticos)**: `src/previews/serve.ts` — `handlePreviewRequest(method, urlPath, res, {previewsRoot})` roteado em `index.ts` **antes** do fallback estático do admin (senão `/previews/...` inexistente cairia no SPA fallback com 200+HTML). URL: `GET /previews/<comp>/<variant>/<sha>/<...arquivo>`; **sem auth por decisão** (preview é conteúdo público, igual à doc). `Cache-Control: public, max-age=31536000, immutable` — o path já é endereçado por commitSha. Diretório → tenta `index.html` dentro (conveniência para `<entryDir>/`). Só GET/HEAD (405 no resto); erro sempre JSON, 404 para arquivo ausente.
- **Helper compartilhado `src/previews/paths.ts`** (nota técnica da TASK-46): `isSafeSegment` (movido de `upload.ts`) + `resolvePreviewPath(root, segments)` que valida cada segmento e confere `resolve(...).startsWith(root + sep)`; usado pelo upload **e** pela rota de leitura, sem duplicar a proteção de traversal. `decodeURIComponent` malformado vira 400, não 500.
- **Nota de traversal**: `new URL()` do Node já normaliza `..` no pathname antes do handler, então traversal cru costuma virar 404 em vez de 400 — os testes aceitam ambos e o que travam é "nunca 200 com arquivo de fora" (verificado com `curl --path-as-is` contra server buildado, além dos 10 testes vitest).
- **Gotcha de teste com http server + fetch**: `server.close()` no `afterEach` fica ~3s esperando o socket keep-alive do `fetch` anterior expirar — chamar `server.closeAllConnections()` antes derruba a suíte de 6s para 0,5s.
- **TASK-42 (component_previews)**: migration 0006 — `component_previews (id, component_name, variant_id, commit_sha, path_estatico, publicado_em default unixepoch)`, índice composto `(component_name, variant_id, publicado_em)` para o lookup de latest. **Sem unique** no par (componente, variante): append-only deliberado, cada upload de CI é linha nova e o `getLatestPreview` resolve "latest wins" com desempate por rowid (unixepoch tem resolução de segundo — mesma lição do listByPage da TASK-35). Helpers em `src/db/componentPreviews.ts`: `insertComponentPreview` (ponto de escrita da TASK-43) e `getLatestPreview` (lookup da TASK-47/46).

## Fase 5 — Integração do preview (em andamento, branch `feature/fase-5-integracao-preview`)

- **TASK-47 (component-embed com iframe real)**: o NodeView (`apps/admin/src/features/editor/nodes/ComponentEmbed.tsx`, ainda `.tsx`) passou a resolver o artefato via `componentPreviews.getLatest` e renderizar `<iframe>` quando `componentName` **e** `variantId` estão setados. Forma persistida do nó inalterada (atoms TASK-29). Estados expostos em `data-preview-state` para E2E: `unset` (falta seleção) / `loading` / `empty` (sem artefato ou erro) / `live` (iframe). Empty-state e loading reusam a caixa tracejada da TASK-29 (polimento fica na TASK-51).
- **Router novo `componentPreviews.getLatest`** (`apps/server/src/trpc/routers/componentPreviews.ts`, protectedProcedure — conteúdo do editor, admin+editor): usa `getLatestPreview` (TASK-42) e resolve a URL servível. Registrado no `router.ts` + matriz de permissões atualizada.
- **Resolução do entryDir em disco** (`apps/server/src/previews/entry.ts`, `resolvePreviewEntry`): o par (componente, variante) **não conhece** o slug do `entryDir` (ex.: `button--primary`), então o DB só guarda `path_estatico = comp/variant/sha`. O helper lê o artefato extraído e acha o `index.html`: primeiro na raiz (layout plano), senão o único subdiretório que o contém. URL final: `/previews/<comp>/<variant>/<sha>/<entryDir>/index.html`. Se o registro existe mas os arquivos sumiram (volume recriado) → retorna null → placeholder, nunca iframe quebrado.
- **`previewsRoot` no contexto tRPC** (`context.ts`, **opcional**): `createContext` ganhou 4º parâmetro `previewsRoot`, injetado com `env.PREVIEWS_PATH` no `index.ts`. Opcional para não quebrar os ~8 testes que forjam contexto inline (`{ db, res, user }`); só o teste de componentPreviews o fornece. `getLatest` lança INTERNAL_SERVER_ERROR se ausente (não ocorre em prod).
- **⚠️ Gotcha crítico de CORS + iframe sandbox** (descoberto e corrigido na verificação Playwright): o iframe usa `sandbox="allow-scripts"` **sem** `allow-same-origin` (política documentada no código: allow-scripts é obrigatório p/ rodar o bundle React de terceiros; same-origin omitido p/ negar acesso a cookies/DOM do painel). Isso dá ao iframe **origem opaca**, e scripts `type="module"` são **sempre** buscados em modo CORS → de origem opaca (`Origin: null`) o browser exige `Access-Control-Allow-Origin` no asset, senão **bloqueia o bundle e o iframe fica em branco**. Fix: `access-control-allow-origin: *` na rota `GET /previews/*` (`serve.ts`) — seguro porque preview é público sem segredos. Sem esse header o preview simplesmente não renderiza dentro do sandbox. Teste da serve.test.ts asserta o header.
- **Sizing do iframe**: altura padrão 240px, `resize: vertical` + `overflow: auto` no `.sb-component-embed-frame` (resize exige overflow != visible). Auto-resize via postMessage ficou como enhancement futuro.
- **Verificação E2E (scratchpad, `e2e/verify.mjs`)**: fluxo TASK-41→43→47 **real** — build do connector na fixture `sample-repo` (Button/primary + Card/default), upload via `POST /api/previews` com token real, inserção do embed no editor via `window.systembookEditor.chain().insertContent({type:'componentEmbed', attrs})`, e checagem no Chromium: iframe de Button/primary renderiza o botão "Salvar", Card/default (sem artefato) cai no placeholder `empty` sem iframe, sandbox correto, altura ~240px, zero erros de console. **Playwright acessa o frame de origem opaca normalmente** (CDP, não same-origin) — dá pra ler o texto renderizado dentro do iframe.
- **TASK-48 (picker de componente/variante)**: `componentPreviews.listComponents` (distinct `component_name`) e `listVariants({componentName})` (distinct `variant_id`) — helpers `listComponentNames`/`listVariantIds` em `db/componentPreviews.ts` via `selectDistinct`. **Fonte de dados é só `component_previews`**: não há registro separado de componentes, um componente só aparece no picker após o primeiro upload de CI (nota do spec). `ComponentEmbedPicker.tsx` é um modal controlado (estilos inline, padrão do codebase) em 2 passos: lista filtrável de nomes → variantes do componente escolhido; confirma com `onConfirm({componentName, variantId})`. Fecha no Esc e no clique no overlay (`onMouseDown` no backdrop, `stopPropagation` no painel). Seletores E2E: `[data-testid=component-embed-picker]`, `[data-component-option=<nome>]`, `[data-variant-option=<id>]`, `[data-testid=component-embed-reselect]`.
- **Toolbar (step 3)**: o botão "🧩 Embed" agora abre o picker (`pickerOpen` state) em vez de inserir attrs vazias; `onConfirm` insere o nó com a seleção. **Re-seleção (step 4)**: o NodeView do ComponentEmbed ganhou um controle "Trocar componente"/"Selecionar componente" (presente em todos os estados: unset/loading/empty/live) que reabre o mesmo picker **pré-preenchido** (`initial`) e atualiza via `updateAttributes` no confirm — a variante atual aparece marcada "(atual)". O picker é usado nos dois lugares (toolbar e NodeView), ambos dentro do TRPCProvider.
- **Verificação E2E (scratchpad, `e2e/verify48.mjs`)**: 3 artefatos publicados (Button/primary, Button/disabled, Card/default). 10 checks no Chromium: picker abre pela toolbar, lista os 2 componentes, filtro "card" reduz a 1, variantes de Button listadas, seleção Button/primary insere o embed e renderiza "Salvar", reabrir o picker sobre o embed mostra "primary (atual)", trocar para "disabled" atualiza os attrs e re-renderiza o iframe (botão `disabled`), zero erros de console. **Gotcha de teste**: a lista de componentes carrega via query async — esperar `[data-component-option]` aparecer antes de contar (o primeiro count dava 0).
- **TASK-49 (painel de controles via postMessage)**: `ControlsPanel.tsx` renderiza um input por `PreviewControl` (text=input / boolean=checkbox / select=`<select>`), colapsável (**default colapsado**, toggle "Controles (N)"). A cada `onChange` envia `{type:'systembook:update-props', props:{[propName]:value}}` ao `iframe.contentWindow.postMessage` — contrato do preview-kit (TASK-38), que mescla e re-renderiza sem reload. Valores iniciais vêm de `variant.props[propName] ?? control.defaultValue ?? default-por-kind`. O painel é montado com `key={variantId}` no NodeView para **re-semear** ao trocar de variante (o iframe recarrega com novas props).
- **De onde vem a config (step 1)**: o `component_previews` não guardava a `PreviewConfig`, e o upload (TASK-43) só recebe component/variant/sha+tar. Solução: o **connector grava `preview-config.json` DENTRO de cada `entryDir` do dist** (build.ts, pós-`viteBuild` — o Vite não copia arquivos arbitrários do root). Como o tar de cada variante é `<entryDir> assets` (docs/ci-example.md), o JSON viaja junto e é extraído ao lado do `index.html`. `GeneratedEntry` ganhou o campo `config`. No server, `readPreviewConfig` (em `previews/entry.ts`) lê `<pathEstatico>/<dirname(entry)>/preview-config.json` e `componentPreviews.getLatest` passou a retornar `config: PreviewConfig | null` (null em artefatos pré-TASK-49 → painel some).
- **⚠️ targetOrigin '*' é obrigatório aqui** (tensão com a nota do spec que pede origin explícita): o iframe usa `sandbox="allow-scripts"` **sem** `allow-same-origin` (TASK-47, segurança), logo tem **origem opaca** — um `targetOrigin` concreto nunca casaria e a mensagem seria descartada pelo browser. Manter a segurança (origem opaca) e ter targetOrigin concreto são mutuamente exclusivos. Usamos '*', mas: (a) o envio é para um `contentWindow` específico, não broadcast; (b) as props são valores de demo não sensíveis; (c) **a proteção real está do outro lado** — o `mount()` do preview-kit valida `event.origin === allowedOrigin` (a origin do referrer, i.e. a do painel admin), então uma página maliciosa em outro iframe não consegue injetar props. Documentado no `ControlsPanel.tsx`.
- **Verificação E2E (scratchpad, `e2e/verify49.mjs`)**: para o `select` ser visualmente asseverável, a fixture Button ganhou `data-variant={props.variant}` **só durante o teste** (revertido depois — não faz parte do entregável). 9 checks: painel colapsado por padrão, expande e mostra 3 controles, controle text muda o texto do botão, select muda `data-variant`, boolean desabilita o botão, props mescladas mantêm o texto custom (prova de que não há reload), zero erros de console.
- **TASK-50 (doc pública read-only)**: `revisions.getLatestPublished` (**publicProcedure**, `input {pageId}`) retorna o `PageSnapshot` da última revisão publicada (mesma ordenação/desempate do `listByPage`) ou `null` — **lê de `revisions`, nunca de `blocks` ao vivo** (concretiza a dependência de ordenação da TASK-34). `componentPreviews.getLatest` **virou publicProcedure** também (o embed precisa dele deslogado; o artefato já era público via /previews/*). `listComponents`/`listVariants` continuam protegidos (picker do editor). Matriz em router.ts atualizada; `permissions.test.ts` não referencia essas rotas (nada quebrou).
- **`PageRenderer.tsx`** (`features/public/`): renderer read-only compartilhado — monta `editorExtensions` com `editable:false` + `blocksToTiptapDoc`, com seletor de tab (só aparece se >1 tab). `RevisionSnapshotPreview` (TASK-35) foi **refatorado para delegar** a ele (DRY — a mesma renderização nos dois lugares). Os NodeViews React renderizam igual em read-only; o `ComponentEmbed` passou a ler `editor.isEditable` (de `NodeViewProps`) para **esconder a (re)seleção** ("Trocar componente"/picker) mantendo o iframe **e o painel de controles interativo** — decisão: controles ficam no público (demo interativa é feature), só a edição some.
- **Rota `/p/:pageId`** (`pages/PublicPage.tsx`): registrada em `main.tsx` **fora do `AdminLayout`** (sem auth, sem chrome de admin). Provisória — a hierarquia por slug (`/:section/:page`) e o layout público vêm na TASK-52 (Fase 6). Estado "não publicada" (`data-testid=not-published`) quando `getLatestPublished` é null. O SPA fallback do server (static.ts) serve o index.html para `/p/*`, e o React Router assume.
- **Verificação E2E (scratchpad, `e2e/verify50.mjs` + `seed50.mjs`)**: doc com os 7 tipos de bloco (heading/paragraph+bold/list/code/table/callout/component-embed) via `saveDraft`+`publish`, depois **contexto de browser novo e limpo (deslogado)**. 14 checks: `/p/:id` não redireciona a /login, os 7 tipos renderizam, o iframe do embed mostra o botão, **sem** "Trocar componente" mas **com** painel de controles, e `/p/:idNuncaPublicada` mostra o estado "não publicada" sem redirect. Zero erros de console.
- **TASK-51 (empty-states do embed, UI-only)**: os três estados do component-embed agora são inequívocos. `unset` (nunca selecionado): caixa cinza tracejada + 🧩 + "Nenhum componente selecionado". `empty` (selecionado mas sem preview publicável — nunca publicado ou referência renomeada/defasada): caixa **âmbar** (`.sb-component-embed--empty`) + **⚠️** + nomeia `component / variant` + **"Tentar novamente"** que faz `previewQuery.refetch()` (útil logo após o CI publicar). `live`: iframe. Nunca renderiza `<iframe>` com src vazio. O botão retry mostra "Verificando…" e fica `disabled` enquanto `isFetching`. **Reuso no público é automático** (mesmo NodeView): retry e re-seleção ficam atrás de `editor.isEditable`, então na doc read-only o empty-state aparece só com a mensagem. Estilo dos botões inline extraído para `emptyActionStyle`.
- **Gotchas do E2E da TASK-51** (`e2e/verify51.mjs`): (1) inserir um segundo `componentEmbed` (atom) com `focus('end')` quando a NodeSelection do primeiro atom está ativa **substitui** o primeiro — usar tabs separadas para comparar `empty` × `unset`; (2) navegar para outra tab e **voltar** dispara o race conhecido do autosave (flush no unmount × getByTab na remontagem, pendência #2) e o embed pode não reaparecer — fazer o fluxo empty+retry **na mesma visita**, sem navegar de volta; (3) o upload de artefato no meio do teste (via `execSync` de curl) **não** faz o embed auto-resolver (a query cacheou null) — confirmado que só o clique em "Tentar novamente" re-busca.

## Fase 6 — Publicação e polimento (em andamento, branch `feature/fase-6-layout-publico`)

- **TASK-52 (layout público navegável)**: árvore de rotas `/docs` **separada do `AdminLayout`** em `main.tsx` (sem auth, sem chrome de admin) — `PublicLayout` (shell: header + sidebar + `<Outlet>`), `PublicHome` (index → redirect à 1ª página publicada, ou estado vazio), `PublicPageView` (`:sectionSlug/:pageSlug/:tabId?`). Tudo em `apps/admin/src/features/public/`. A rota antiga `/p/:pageId` (`PublicPage`, TASK-50) **foi mantida** como link direto por id (bookmark/preview sem slug).
- **Slug de section (decisão de modelagem)**: sections não tinham slug; a URL `/docs/:sectionSlug/...` exige um. Adicionado `slug` a `sections` (**migration 0008**: `ALTER TABLE ADD COLUMN` nullable + `CREATE UNIQUE INDEX` — sem rebuild, respeita "nunca editar migration à mão"). Populado pela aplicação: `sections.create` gera via `generateUniqueSectionSlug` (slugify do título + desambiguação `-2/-3`), e `backfillSectionSlugs(db)` no boot (`index.ts`, idempotente, só toca `slug IS NULL`) cobre linhas legadas. `slugify`/geradores em `apps/server/src/db/sections.ts`. **Rename NÃO altera o slug** (estabilidade da URL — critério do spec); slug editável manualmente fica como enhancement futuro.
- **Queries públicas** (`publicProcedure`, matriz em router.ts atualizada): `sections.listPublic` retorna a **árvore aninhada** (seções com ≥1 página publicada, cada uma com suas páginas publicadas) — um round-trip, sem N+1; "publicada" = página com ≥1 revisão (`EXISTS` em revisions). Desvio deliberado do `listPublic`/`listPublicBySection` separados do spec. `pages.getPublishedBySlug({sectionSlug, pageSlug})` resolve via join sections↔pages e retorna `{pageId, titulo, snapshot}` (snapshot = última revisão, mesmo desempate do `getLatestPublished`; `null` se seção/página não existe → 404; snapshot `null` se nunca publicada → estado "não publicada").
- **Sidebar** (`PublicSidebar.tsx`): seções (headers) + páginas (`NavLink` para `/docs/:sec/:page`), destaque da ativa via classe `.active` do NavLink (ativo também nos descendentes `/:tabId`, pois NavLink não é `end`). Tabs **não** vão na sidebar — aparecem no header da página (o seletor do `PageRenderer`).
- **`PageRenderer` ganhou modo controlado** (`activeTabId`/`onSelectTab` opcionais): a doc pública reflete a tab na URL (`/:tabId`) e trocar de tab faz `navigate` client-side; sem esses props ele gerencia a tab internamente (o `RevisionSnapshotPreview` continua igual).
- **Verificação E2E (scratchpad, `e2e/verify52.mjs`)**: seed com 2 seções publicadas + 1 rascunho + 1 seção vazia. **Deslogado**, 17 checks: `/docs` redireciona à 1ª página, sidebar lista só o publicado (rascunho/seção-vazia ocultos), destaque da ativa, navegação client-side sem full reload (marker `window` preservado), tabs no header com URL sincronizada, link direto, 404, **nenhum chrome de admin** (sem Publicar/Sair/toolbar), zero erros de console. **Gotchas de teste**: (1) `text-transform: uppercase` no CSS faz `innerText` vir MAIÚSCULO — comparar em lowercase; (2) navegação client-side dispara query async — esperar o novo conteúdo (`locator hasText`) antes de asseverar, senão lê o conteúdo antigo.

- **TASK-53 (busca full-text FTS5)**: virtual table `pages_fts USING fts5(page_id UNINDEXED, titulo, section_titulo, conteudo)` via **migration raw 0009** (`0009_faithful_fts5_search.sql` + entrada no `_journal.json` idx 9). FTS5 **não é declarável no schema Drizzle** — foi o caso legítimo de escrever migration à mão (o `db:generate` não geraria virtual table); toda interação é SQL cru via `db.run`/`db.all(sql\`…\`)`. FTS5 vem compilado no better-sqlite3 por padrão (verificado com `char()`/`snippet()`).
  - **`src/db/search.ts`** (novo): `extractSearchableText(snapshot)` anda todas as tabs/blocks e concatena o texto plano só de `heading/paragraph/list/callout/table` (pula `code/image/component-embed`), coletando recursivamente `node.text` do JSON Tiptap (`.body`). `reindexPageFts(db|tx, pageId, snapshot)` busca titulo da página+seção (innerJoin) e faz **delete+insert** na `pages_fts` (FTS5 não faz UPDATE bem). `searchPublishedPages(db, q, limit=20)` roda `... WHERE pages_fts MATCH ? ORDER BY rank`, com JOIN de volta a `pages`/`sections` (que também **descarta órfãos** de páginas deletadas — virtual table não tem FK cascade).
  - **Reindex nos pontos de escrita únicos**: `createRevision` (publish, TASK-34) reindexa após inserir a revisão; `restoreRevision` (TASK-36) reindexa **dentro da transação** com o `restoredSnapshot` (a busca reflete o conteúdo restaurado, não o anterior). Nunca-publicadas nunca ganham linha → excluídas automaticamente.
  - **Query MATCH segura**: `buildMatchExpression` tokeniza em `\p{L}\p{N}_`, **cita cada termo** (`"term"*`) neutralizando sintaxe FTS5 (`"`, `(`, `AND`, `*`) e adiciona prefixo. `q` só com símbolos → `[]` (sem erro de sintaxe). `search.query` é **publicProcedure** (busca da doc pública), matriz em router.ts atualizada.
  - **Snippet sem injeção de HTML**: o `snippet()` **não** escapa o texto do conteúdo (untrusted), então NÃO usei `<mark>` no server — os delimitadores são os caracteres de controle **STX (char(2)) / ETX (char(3))**; o cliente (`highlight()` em `PublicSearch.tsx`) escapa via React e envolve só os trechos casados em `<mark>` (zero `dangerouslySetInnerHTML`). Exports `SNIPPET_MATCH_OPEN/CLOSE` documentam os delimitadores.
  - **UI pública (além do spec — não havia task separada de UI de busca na Fase 6)**: rota `/docs/search?q=…` (`PublicSearch.tsx`) lista resultados rankeados com snippet destacado + link para `/docs/:sectionSlug/:pageSlug`; caixa de busca no header do `PublicLayout` (`PublicSearchBox.tsx`) navega no submit e sincroniza com `?q`. Estilos em `public.css`.
  - **Verificação**: 5 testes vitest (`src/trpc/search.test.ts`: acha por texto/título/seção, exclui rascunho, restore reindexa, `q` simbólico não quebra) + **E2E Playwright deslogado** (scratchpad `verify53.mjs` + `seed53.mjs`, server buildado na porta 3253): 11 checks — 1 resultado p/ termo distinto (draft não publicado excluído), snippet com `<mark>`, link correto, no-results, caixa do header submete e navega, clique abre a doc, zero erros de console.

- **TASK-54 (UI de busca em dropdown ao vivo)**: `apps/admin/src/features/public/SearchBox.tsx` no header do `PublicLayout` — input controlado com **debounce de 300ms** (`query` → `debounced` via `setTimeout`), `useQuery(trpc.search.query…, {enabled: debounced.length>0})`, dropdown `position:absolute` com resultados (título + breadcrumb de seção + snippet destacado). Navegação por **clique** (`onMouseDown`+`preventDefault` para navegar antes do blur fechar o dropdown) e **teclado** (↑/↓ move `active`, Enter navega); fecha no Escape, clique-fora (listener de `mousedown` no document) e ao selecionar. Estados: query vazia → sem dropdown; sem match → "Nenhum resultado encontrado".
  - **Escopo MVP (nota do spec)**: dropdown leve in-page, **sem página de resultados dedicada** — por isso **removi a página `/docs/search` e o `PublicSearchBox` que eu tinha adiantado na TASK-53** (a rota `search` saiu do `main.tsx`). O `search.query` do backend (TASK-53) ficou intacto.
  - **⚠️ Gotcha crítico (custou depuração): caracteres de controle literais no fonte somem ao salvar.** As sentinelas STX/ETX do snippet, escritas como caracteres literais em `SearchBox.tsx`, viraram **string vazia** ao gravar o arquivo (o server manteve as suas, o admin não — inconsistente). Com `MATCH_OPEN=''`/`MATCH_CLOSE=''`, o regex de destaque virou `([^]*)` com flag `g` → casa vazio no fim → **`regex.exec` em loop infinito**, congelando a thread do browser (o Playwright travava no `evaluate`, sem erro). **Fix**: `const MATCH_OPEN = String.fromCharCode(2)` / `String.fromCharCode(3)` (nunca caractere de controle literal no source) **+** guarda anti-loop no `while` (`if (regex.lastIndex === match.index) regex.lastIndex++`). Lição: sempre representar STX/ETX/afins via `String.fromCharCode`/`\u00xx`, jamais literal.
  - **Verificação**: 12 checks Playwright deslogado (`verify54.mjs` + `seed53.mjs`, server buildado porta 3254): digitar não navega por tecla, 1 resultado após debounce, título/seção/`<mark>`, clique navega e fecha o dropdown, no-results, query vazia sem dropdown, teclado ↓+Enter navega, zero erros de console.

- **TASK-55 (tema claro/escuro + tipografia)**: sistema de **design tokens em CSS custom properties** no root `.sb-public` (`public.css`) — tema claro é o padrão, `.sb-public[data-theme='dark']` redefine os tokens (`--sb-bg/-fg/-border/-accent/-code-bg/-th-bg/-callout-*` etc.). Toggle sol/lua no header via `useTheme.ts` (`localStorage['sb-theme']` > `prefers-color-scheme` no 1º render; troca instantânea = state → `data-theme` no div `.sb-public`, sem reload; try/catch no localStorage p/ modo privado). Tipografia da prosa: `.sb-public .sb-editor .ProseMirror` **remove o chrome de editor** (borda/min-height/padding), escala de headings h1–h3, h2 com regra inferior, `line-height 1.7`, `max-width 860px`, links no accent.
  - **⚠️ Gotcha das cores de callout (inline vencia o tema)**: o NodeView do Callout (`nodes/Callout.tsx`) aplicava `border`/`background` **inline** por variante — estilo inline tem specificity máxima e o tema por CSS nunca venceria (nem via custom property setada inline no mesmo elemento). **Fix**: removi o `style` inline do NodeView e movi as cores para `editor.css` por `[data-variant]` (via `--sb-callout-border/-bg` com fallback = look claro atual, **admin inalterado** pois não seta `data-theme`); o tema escuro sobrescreve `--sb-callout-*` + `color` por variante em `public.css` com specificity maior (`.sb-public[data-theme=dark] .sb-editor .sb-callout[data-variant=…]`). Mesmo princípio para code/tabela: `editor.css` passou a ler `var(--sb-code-bg/-fg, fallback)` / `var(--sb-th-bg/-border, fallback)`, e os tokens cascatam do root `.sb-public` por tema. **Lição**: para algo tematizável por CSS, nunca fixar a cor inline no componente.
  - **Escopo (nota do spec)**: só a experiência de leitura pública é tematizada; o painel admin não precisa acompanhar (e não acompanha — não seta `data-theme`, usa os fallbacks claros).
  - **Verificação**: 10 checks Playwright (`verify55.mjs` + `seed55.mjs` com os tipos de bloco, porta 3255): contexto `colorScheme:'light'` sem storage → tema claro; toggle → escuro instantâneo + `localStorage` gravado + bg muda; reload mantém a escolha; contexto `colorScheme:'dark'` sem storage → escuro inicial (respeita o sistema). **Screenshots claro/escuro inspecionados** — callouts info/aviso/dica distintos e legíveis, code block e tabela theme-aware nos dois modos.

- **TASK-56 (página inicial customizável)**: em vez de um storage paralelo, a landing **reusa toda a máquina de tabs/blocks/revisions** (serialização TASK-31, autosave TASK-32, publish TASK-34) via uma **section/page/tab reservadas** com ids sentinela fixos (`db/landing.ts`: `LANDING_SECTION_ID/PAGE_ID/TAB_ID` = strings `__sb_landing_*__`; a section tem slug reservado não-nulo p/ o backfill de slugs não tocá-la). `ensureLandingPage(db)` (idempotente via `onConflictDoNothing`, chamado no boot em `index.ts` após o backfill de slugs) cria as 3 linhas. **Isolamento** (a landing não é doc "normal"): oculta de `sections.list` (árvore do admin) e `sections.listPublic` (nav pública) via `ne(id, LANDING_*)`, e **fora do índice FTS** com um guard no topo de `reindexPageFts` (`if pageId === LANDING_PAGE_ID return`). A URL `/docs/__sb_landing__/__sb_landing__` ainda renderiza (conteúdo público de qualquer forma) — aceito, não vale excluir.
  - **Router `landing`** (`routers/landing.ts`): `get` (publicProcedure) → snapshot da última revisão publicada da landing ou `null`; `getEditorTarget` (protectedProcedure) → `{pageId, tabId}` reservados (o client não hardcoda os sentinelas). Registrado no `router.ts` + matriz.
  - **Admin**: rota `/admin/settings/landing-page` (`pages/LandingPageSettingsPage.tsx`) reusa `ContentEditor` apontado para o tab reservado + botão Publicar via `pages.publish(LANDING_PAGE_ID)` (mesmo padrão do `TabContentPage`, com `flush()` antes). Link "Página inicial" no header do `AdminLayout` (visível a admin **e** editor — é conteúdo). **Público**: `PublicHome` (`/docs`) deixou de redirecionar para a 1ª seção; agora renderiza a landing publicada via `PageRenderer` (`data-testid=landing-published`) ou um **estado padrão** (`landing-default`) de boas-vindas com link para a 1ª página publicada quando a landing nunca foi publicada.
  - **Verificação**: 4 testes vitest (`landing.test.ts`: idempotência, get null→snapshot, isolamento de busca/nav, auth) + **12 checks Playwright com o fluxo admin REAL** (`seed56.mjs` cria admin com `hashPassword` + 1 página normal publicada; server buildado porta 3256): deslogado vê o estado padrão com link (sem redirect); login admin → `/admin/settings/landing-page` carrega o editor → conteúdo inserido via `window.systembookEditor.chain()` → Publicar → feedback de sucesso; deslogado de novo vê a landing publicada renderizada e **sem vazar para a sidebar**; zero erros de console.

- **TASK-57 (layout responsivo, UI-only)**: breakpoint **768px** na doc pública. **<768px**: a `.sb-public-sidebar` vira **drawer off-canvas** (`position:fixed; translateX(-100%)`, slide-in com `data-open`), aberto por um **hambúrguer** (`.sb-nav-toggle`) no header, com **backdrop** (`.sb-public-backdrop`, fixed, z abaixo do drawer); clicar num link ou no backdrop fecha (`onNavigate`/`setNavOpen(false)`). A **busca** (`SearchBox`) ganhou modo mobile: um **gatilho-ícone** (`.sb-search-trigger`) abre um **overlay full-screen** (`.sb-searchbox[data-mobile-open]` = `position:fixed; inset:0`) com input full-width + botão fechar (`.sb-search-close`); no desktop segue o input inline + dropdown. Alvos de toque ≥44px no mobile. `PublicLayout` ganhou `navOpen` state; `PublicSidebar` ganhou props `open`/`onNavigate`.
  - **Conteúdo largo contido** (nunca scroll horizontal da página): Tiptap já envolve tabelas em `.tableWrapper` → `overflow-x:auto`. **Gotcha da tabela**: o `editor.css` usa `table-layout:fixed; width:100%`, que no mobile **espremia** as colunas (quebra de texto) em vez de rolar. Fix escopado a `.sb-public`: `table-layout:auto; width:auto; min-width:100%` + `th/td { white-space:nowrap }` → colunas legíveis e o excedente rola no `.tableWrapper` (o editor admin mantém o fixed/resizable). `img/iframe { max-width:100% }`, `pre` já tinha overflow.
  - **Verificação**: 19 checks Playwright em 2 viewports (`verify57.mjs` + `seed57.mjs` com tabela de 8 colunas, porta 3257) — desktop 1440×900: hambúrguer escondido, sidebar permanente, input inline, zero scroll horizontal; mobile 390×844: hambúrguer abre/fecha o drawer (backdrop + link), sem scroll horizontal da página, **tabela rola no wrapper** (`scrollWidth>clientWidth`), busca vira overlay full-width com resultados e botão fechar, alvo de toque ≥44px, zero erros de console. **Gotcha de teste**: clicar no backdrop via testid falha (o centro do elemento full-screen cai sob o drawer de 300px) — clicar por coordenada `mouse.click(365,400)` na área descoberta.

## Fase 7 — Empacotamento e lançamento (em andamento, branch `feature/fase-7-empacotamento`)

- **TASK-58 (publish da imagem Docker no GHCR)**: `.github/workflows/docker-publish.yml` — builda o `Dockerfile` da raiz e publica em `ghcr.io/${{ github.repository }}` (= `ghcr.io/mateusvillain/systembook`, minúsculo, correto em forks). **GHCR** escolhido por não exigir conta/segredo além do repo — auth via `GITHUB_TOKEN` (`permissions: packages: write`). Dispara em **tags `v*`** (versão semver `{{version}}` + `{{major}}.{{minor}}` + `latest`) e em **push na `main`** (tag `edge`); `workflow_dispatch` manual. `docker/metadata-action` deriva tags/labels; `push` só ocorre quando há tags a aplicar (`steps.meta.outputs.tags != ''`) — build-sem-push em branch/dispatch sem tag. Cache de camadas `type=gha` (`cache-from`/`cache-to: mode=max`).
  - **⚠️ Passo manual único** (documentado no topo do workflow): pacotes do GHCR nascem **privados** mesmo em repo público — após o 1º publish, tornar o pacote público em Repo → Packages → systembook → Package settings → Change visibility, senão o `docker pull` de terceiros exige auth.
  - **Verificação local (o que dá sem release)**: `docker build` da raiz OK; container roda migrations, serve o SPA admin e o `/trpc/health.check` responde `{status:ok,db:ok}` com as env vars documentadas (`SESSION_SECRET`/`ARGON2_SECRET`/`INITIAL_ADMIN_EMAIL`/`INITIAL_ADMIN_PASSWORD`). YAML validado.
  - **Release real verificado (step 4 ✅)**: tag **`v0.1.0`** empurrada (no HEAD da branch da Fase 7 — a `main` ainda não tem o workflow; releases futuros, pós-merge, tagueiam a `main`) → workflow verde em ~1m15s → imagem publicada em `ghcr.io/mateusvillain/systembook` (tags `0.1.0`/`0.1`/`latest`). Round-trip confirmado: `docker login ghcr.io` com `gh auth token`, `docker pull --platform linux/amd64`, `docker run` → `/trpc/health.check` = `{status:ok,db:ok}`. **TASK-58 = passes:true.**
  - **Follow-up (a) — RESOLVIDO**: o usuário tornou o pacote **público** nas Package settings; **pull anônimo (deslogado) confirmado** (`docker logout ghcr.io` + `docker pull --platform linux/amd64 ghcr.io/mateusvillain/systembook:0.1.0` → Pull complete). Critério "imagem pullável por terceiros" fechado.
  - **Follow-up (b) — RESOLVIDO (multi-arch adicionado)**: workflow agora tem `docker/setup-qemu-action` + `platforms: linux/amd64,linux/arm64` no build-push. **Validado sem publicar** via `workflow_dispatch` na branch (sem tag → `push:false`, mas as duas arquiteturas compilam): run verde, incl. `better-sqlite3` nativo em arm64 sob QEMU. ⚠️ **`v0.1.0` continua amd64-only** (foi buildado antes); o **primeiro publish multi-arch sai no próximo tag `v*`**. Truque útil: dispatch na branch sem tag builda todas as plataformas sem empurrar — bom p/ validar o build de release sem cortar release.
  - **Node 20 deprecation warning** no run (checkout/build-push/etc. targetam Node 20, forçados p/ Node 24) — informativo, não bloqueia; some quando as actions atualizarem major.
  - **Cross-link**: o critério 5 (README referencia a imagem publicada) é entregue pela **TASK-62** (README).
- **TASK-59 (compose de produção)**: `docker-compose.production.yml` na raiz (distinto do `docker-compose.yml` de dev) — usa a **imagem publicada** `ghcr.io/mateusvillain/systembook:latest` (comentário sugere fixar versão), `env_file: .env`, volume nomeado `systembook-data` em `/app/data` (onde vivem SQLite + previews), `restart: unless-stopped`, e **healthcheck** batendo `/trpc/health.check`. **Gotcha do healthcheck**: `node:24-slim` **não tem wget nem curl** — o teste usa o próprio `node` (`fetch(...).then(r=>process.exit(r.ok?0:1))`; Node 24 tem `fetch` global). Companion `.env.production.example` (commitado; documenta os 6 vars — obrigatórios SESSION_SECRET/ARGON2_SECRET/INITIAL_ADMIN_EMAIL/INITIAL_ADMIN_PASSWORD, opcionais PORT/DATABASE_PATH/PREVIEWS_PATH com default na imagem). `.gitignore` já cobre `.env`/`.env.local`/`.env.*.local`; o `.example` não casa nenhuma regra → commitado.
  - **Nota**: `PORT` no mapeamento `${PORT:-3000}:${PORT:-3000}` funciona porque o Compose auto-carrega o mesmo `.env` para interpolação (o arquivo se chama `.env`, o default do Compose) **e** o injeta no container via `env_file`.
  - **Verificação (step 4) contra a imagem REAL do GHCR** (dir isolado no scratchpad, `DOCKER_DEFAULT_PLATFORM=linux/amd64` só p/ este Mac ARM já que `v0.1.0` é amd64-only): `docker compose up -d` puxou a imagem publicada, o **healthcheck do Compose reportou `healthy`**, host respondeu `{status:ok,db:ok}`, 1º boot semeou o admin; após `down` (sem `-v`) + `up`, **o seed pulou** (log sem "Admin de bootstrap criado") — prova que o volume persistiu os dados. Teardown com `-v`.
- **TASK-63 (LICENSE + CONTRIBUTING)** — feita fora de ordem (antes de 60/61/62), a pedido do usuário. Licença **MIT** confirmada pelo dono via AskUserQuestion (copyright **2026 Mateus Villain**). `LICENSE` na raiz + campo `"license": "MIT"` nos **6** `package.json` (raiz + server/admin + schema/preview-kit/connector; todos `private:true`, mas o campo foi setado). `CONTRIBUTING.md` cobre: pré-requisitos (Node ≥22/pnpm 10), setup local (`.env.local` copiado do `.env.production.example`, dev em 2 processos **ou** `docker compose up` do compose de dev), os 3 checks (`pnpm lint/typecheck/test` = ordem do CI), convenções (Conventional Commits em PT, migrations nunca editadas à mão), e uma seção destacada sobre **`packages/schema` ser contrato cross-cutting** (types-only; espelhos de runtime a manter em sincronia: `BLOCK_TYPES`, zod do `PreviewConfig` no connector, mapeamento block→nó duplicado em serialize.ts × blocksToTiptapDoc.ts). **Step 5 (README linka LICENSE + CONTRIBUTING) pendente da TASK-62** — o README ainda não existe; `pass` do índice e o step 5 ficam `false` até lá.

## Fase 8 — Bloco Dos and Don'ts (COMPLETA, branch `feature/dos-donts-block`)

- **TASK-71 (schema+serialize)**: `DosDontsBlockContent { variant: 'do'|'dont', titulo, descricao: TiptapJson, cover?: DosDontsCover }` em `packages/schema/src/block.ts`. `DosDontsCover` é união discriminada por `kind` (`image` com `src`/`alt`, ou `component-embed` com `componentName`/`variantId`). **Sem migration** — `BLOCK_TYPES` (app-layer, TASK-30) só ganhou `'dos-donts'`. Nó Tiptap `dosDonts` (camelCase) ↔ block `dos-donts` (kebab), mesmo padrão `componentEmbed`/`component-embed`. `descricao` guarda o `content` aninhado (block+), igual ao `callout`. `extractSearchableText` (FTS) tratou `dos-donts` como caso especial (não tem `.body` como os outros — tem `.titulo` + `.descricao`).
- **TASK-72 (NodeView+toolbar)**: `nodes/DosDonts.tsx` — mesmo padrão do `Callout` (switcher in-place, attrs em `data-*`). Título é **input controlado fora do content** (metadado via `updateAttributes`, não ProseMirror content) — diferente do body, que é `NodeViewContent`. **Refatorei `insertCallout` em `EditorToolbar.tsx`** para um helper genérico `insertBlockWithCaret(nodeType, attrs, contentSelector)`, reusado por `insertCallout` e `insertDosDonts` — reduz duplicação da lógica fiddly de posicionamento de caret (NodeView monta assíncrono, mesmo gotcha da TASK-28).
- **TASK-73 (cover opcional)**: `nodes/DosDontsCover.tsx` — add/remove, switch imagem↔component-embed. **Decisão de escopo**: imagem é só URL (`src`/`alt`), sem upload — o projeto não tem pipeline de upload genérico (achado: o bloco `image` do schema em si **nunca foi ligado ao editor**, só existe em serialize.ts). Cover component-embed **reaproveita `ComponentEmbedPicker`** para seleção, mas a resolução do iframe (loading/empty/live via `componentPreviews.getLatest`) foi **duplicada deliberadamente** de `ComponentEmbed.tsx` (comentário no código aponta a razão: extrair um helper alteraria o DOM já validado por E2E das TASK-47/48/51) — sem o `ControlsPanel` interativo, por ser um slot de apoio visual. **Verificado com pipeline real de upload**: gerou token no painel, publicou o artefato `Button/primary` da fixture do connector via `curl` (mesmo fluxo do `docs/ci-example.md`), selecionou no picker, iframe `live` renderizou de verdade.
- **TASK-74 (render público+verificação)**: renderização read-only já funcionava sem código extra (o NodeView já checa `editor.isEditable`) — mas a **verificação achou um gap real**: faltavam os overrides de tema escuro (`--sb-dos-donts-border`/`--sb-dos-donts-bg` sob `.sb-public[data-theme='dark']`) que o `callout` já tinha da TASK-55; sem eles o bloco ficaria sempre com cores de tema claro na doc pública. Corrigido em `public.css`, espelhando exatamente o padrão do callout. E2E completo (Playwright, 10 checks): 3 blocos (do sem cover, do com cover de imagem, dont com cover de component-embed real) → publish → `/docs/...` deslogado → renderização correta + toggle de tema escuro + busca full-text acha texto do título e da descrição de ambos os blocos (incl. o "dont").
- **Flake não-relacionado observado durante a fase**: `pnpm test`/`pnpm -r run test` falha o preview-kit de forma não-determinística com `(0, act) is not a function` — comportamento do runner `pnpm -r`, independe de `--workspace-concurrency`; sempre passa isolado (`pnpm --filter @systembook/preview-kit test`) e o CI (mesmo comando, Linux) vem verde consistentemente. Reexecutar `pnpm test` resolve localmente.
- **Fan-out final**: lint ✓, typecheck ✓, 165 testes (150 server + 7 preview-kit + 8 connector). **MERGEADA na `main`** (PR #9, merge `6b61734`).

## Fase 9 — Modernização visual do painel com Shadcn (MERGEADA na `main`, PR #10, merge `8656c9a`)

Escopo **admin-only** (a doc pública `.sb-public` mantém o tema CSS-vars da TASK-55, intocada).

- **TASK-75 (setup Tailwind v4 + shadcn/ui)**: `@tailwindcss/vite`, alias `@`→src (vite+tsconfig). **Ponto crítico resolvido**: preflight global do Tailwind quebraria a prosa de `.sb-public` e `.sb-editor` (mesmo bundle). Solução em `src/index.css`: importa **só** `tailwindcss/theme.css` + `tailwindcss/utilities.css` (opt-in, não vazam) — **não** o preflight; reset mínimo (box-sizing + borda) **escopado em `.sb-admin`** dentro de `@layer base` (fica **abaixo** do CSS não-layerizado editor.css/public.css, que sempre vencem, e abaixo das utilities). shadcn manual (sem CLI): `components.json`, `lib/utils.ts` (cn), `Button`. `.sb-admin` aplicado em `AdminLayout` + `LoginPage`. Tokens base `neutral` light/dark. Verificado: h1 público mantém 27px (zero vazamento).
- **TASK-76 (lucide + sonner)**: `<Toaster/>` no root; **convenção documentada no main.tsx** (toast = evento transiente: publicar/token/restore/criar usuário; inline `role=alert/status` = estado persistente/de campo — autosave `data-save-status` NUNCA vira toast). Publish do `PageContentPage` convertido p/ toast.
- **TASK-77 (auth+shell)**: `LoginPage` (Card/Input/Label/Button, erro genérico inline), `AdminLayout` (nav NavLink + role-gating intocado, Sair com ícone LogOut).
- **TASK-78 (sidebar)**: `SidebarTree` — constantes inline→classes Tailwind, glifos→ícones lucide (Chevron/Pencil/Arrow/Trash2/Check/X/Plus), ações no hover. Lógica de query/reorder/lazy-load e regras TASK-67/70 intocadas.
- **TASK-79 (chrome do editor)**: `EditorToolbar` (Button + ícones lucide, `insertBlockWithCaret` preservado), `PageContentPage` header/tab-bar, `ComponentEmbedPicker` (modal Tailwind+shadcn), `ControlsPanel` (Input/Switch, postMessage preservado), `DosDontsCover` (Button/Input). **`editor.css`/prosa Tiptap NÃO tocados** (verificado: h1 grande mantido).
- **TASK-80 (telas de gestão)**: componentes `table`/`badge`; `UsersPage`, `UploadTokens` (**reveal-once preservado** — valor só no estado `revealed`, some após reload, verificado), `GlobalHistoryPage`, `PageHistoryPage`/`RevisionHistoryList`, `LandingPageSettingsPage` — todos com toasts na convenção. Smoke-test da TASK-75 removido do Dashboard.
- **TASK-81 (QA final)**: walk por todas as telas (zero erros de página/console), a11y (foco visível/labels/logout), doc pública intocada (sem `.sb-admin`, tipografia própria, tema escuro). Fan-out verde.
- **Componentes shadcn adicionados**: button, card, input, label, sonner, switch, table, badge (+radix slot/label/switch). Base color neutral.
- **Flake conhecido reconfirmado**: `pnpm test`/`pnpm -r run test` falha o preview-kit (`(0, act) is not a function`) de forma **não-determinística mesmo serializado** (`--workspace-concurrency=1`); passa isolado e o CI vem verde. Reexecutar resolve.

## Pendências / próximos passos

0. **Fase 10 — Redesign de interface (Zeroheight)**: backlog criado em 2026-07-21 (TASK-82..93), nenhum step iniciado. Ordem recomendada: 82 (tokens, independente) → **83 (schema+migration do Menu, com backfill de menu padrão) → 84 (CRUD tRPC do Menu)** → 85 (header com nav de menus + criação/gestão de menu — complexidade subiu p/ high) → 86 (sidebar 2 níveis escopada ao menu ativo, remove tabs da árvore — complexidade subiu p/ high) → 87 (área principal/Section Header + breadcrumbs c/ nível Menu) → 88 (toolbar de blocos, a mais técnica) → 89 (menu ⋮ da sidebar, possivelmente reaproveitado pela 85) → 90 (empty states incl. menu sem seções) → 91 (busca admin incl. menus) → 92 (responsivo) → 93 (QA final, com um passo dedicado ao walkthrough do Menu). Ver `plano-de-interface.md` (fonte da verdade do design) e `referencia.png`/`referencia-2.png` (raiz do repo) para calibração visual. **Gap conhecido documentado nas tasks**: duplicar/mover seção-página entre pais e busca por componentes/tokens/usuários não têm suporte no backend hoje — deixados de fora da Fase 10 deliberadamente (não inventar UI sem lastro de API).
1. **Fase 7 em andamento** na branch `feature/fase-7-empacotamento`. **TASK-58/59/63 ✅**. Restam **TASK-60** (docs de setup/instalação), **TASK-61** (doc do schema `*.preview.tsx`), **TASK-62** (README — deve linkar LICENSE + CONTRIBUTING, fechando o step 5 da TASK-63, e referenciar a imagem GHCR, fechando o critério 5 da TASK-58) e **TASK-64** (docs de backup). Ao fim: cortar `v0.1.1` (multi-arch) + PR + merge (PR #5).
   - ⚠️ **TASK-63 tem 1 pendência cruzada**: quando a TASK-62 criar o README, linkar `LICENSE` e `CONTRIBUTING.md`, então marcar o step 5 da TASK-63 e o `passes` do índice como `true`.
   - **⏳ AÇÃO PENDENTE PARA O FIM DA FASE 7 (decisão do usuário em 2026-07-21)**: cortar um **`v0.1.1`** para publicar a **primeira imagem multi-arch** (o `v0.1.0` no GHCR é amd64-only; o workflow já suporta arm64 mas só entra em vigor num tag novo). Ao cortar, validar o pull **nativo arm64** neste Mac (sem `--platform`). O usuário optou por adiar isso para o fechamento da fase, não fazer agora.
0. **Fase 6 MERGEADA** (TASK-52..57) via **PR #4**. Revisão pré-merge pegou 1 regressão (`sections.reorder` × section reservada da landing — corrigida com `ne(id, LANDING_SECTION_ID)` + teste). `db:generate` roda limpo apesar da migration 0009 FTS5 sem snapshot (drizzle-kit ignora a virtual table).
2. Fixes de dev já na `main`: proxy do Vite encaminha `/previews` e `/api/previews` (PR #3) — o embed funciona em `pnpm dev`. Fases 4 e 5 mergeadas (PR #1, #2).
3. Race conhecido (aceito no MVP): flush de autosave no unmount × fetch do `getByTab` na remontagem — em navegação muito rápida ida-e-volta o editor pode abrir sem o último flush (o dado não se perde no banco; basta recarregar).
4. `.pnpm-store/` local (criado pelo container de dev) está no `.gitignore`; pode ser apagado à vontade.

## Avisos de segurança registrados

- `.gitignore` criado em 2026-07-19 cobrindo `.env.local`, `data/`, `*.db`, `.pnpm-store/` — o primeiro commit do repo é ele, garantindo que nenhum segredo entrou no histórico.
- A `INITIAL_ADMIN_PASSWORD` do usuário ficou exposta no chat de uma sessão (arquivo anexado). Recomendado ao usuário: trocar a senha no `.env.local` por uma mais forte e recriar os bancos de dev (`rm -rf apps/server/data` + `docker volume rm systembook_sqlite-data`) antes de uso real; a tela de login já existe, então a troca pode (e deve) ser feita a qualquer momento — ainda **pendente de confirmação do usuário**.
- Os valores de `SESSION_SECRET`/`ARGON2_SECRET` foram gerados via `openssl rand -base64 32` direto no arquivo, sem passar pelo chat/logs.
